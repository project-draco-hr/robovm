{
synchronized (lock) {
    if (isClosed()) {
      throw new IOException(Messages.getString("luni.A5"));
    }
    if (pos < end || fillBuf() != -1) {
      return buf[pos++];
    }
    return -1;
  }
}
