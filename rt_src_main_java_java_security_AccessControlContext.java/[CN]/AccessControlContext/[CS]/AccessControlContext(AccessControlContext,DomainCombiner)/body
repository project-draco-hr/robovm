{
  SecurityManager security=System.getSecurityManager();
  if (security != null)   security.checkPermission(createAccessControlContext);
  this.domainsArray=acc.domainsArray;
  this.domainCombiner=combiner;
}
