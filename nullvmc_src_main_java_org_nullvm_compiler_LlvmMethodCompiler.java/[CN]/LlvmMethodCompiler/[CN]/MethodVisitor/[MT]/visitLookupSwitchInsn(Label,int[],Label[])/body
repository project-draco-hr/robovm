{
  Var op=pop("op");
  out.format("    switch i32 %s, label %%%s [\n",op,dflt);
  for (int i=0; i < keys.length; i++) {
    out.format("        i32 %d, label %%%s\n",keys[i],labels[i]);
  }
  out.format("    ]\n");
}
