{
  if (o == null) {
    return 0L;
  }
  NSString s=new NSString(o);
  try {
    s.retain();
    if ((flags & MarshalerFlags.CALL_TYPE_CALLBACK) > 0) {
      s.autorelease();
    }
    return s.getHandle();
  }
  finally {
    s.dispose();
  }
}
