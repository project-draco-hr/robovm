{
  for (  PHAssetResourceType v : values()) {
    if (v.n == n) {
      return v;
    }
  }
  throw new IllegalArgumentException("No constant with value " + n + " found in "+ PHAssetResourceType.class.getName());
}
