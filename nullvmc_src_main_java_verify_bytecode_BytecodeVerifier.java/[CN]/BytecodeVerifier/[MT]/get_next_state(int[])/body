{
  AbstractState state=unchecked.dequeue();
  if (state != null) {
    pc[0]=state.pc();
    if (pc[0] == -1)     throw new InternalError("next state has pc -1");
    state=state.copy();
    if (state.pc() != -1)     throw new InternalError("copy yields pc != -1");
  }
  return state;
}
