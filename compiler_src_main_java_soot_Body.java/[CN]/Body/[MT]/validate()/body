{
  validateLocals();
  validateTraps();
  validateUnitBoxes();
  validateLocalVariables();
  if (Options.v().debug() || Options.v().validate()) {
    validateUses();
    validateValueBoxes();
    checkInit();
    checkTypes();
    checkLocals();
  }
}
