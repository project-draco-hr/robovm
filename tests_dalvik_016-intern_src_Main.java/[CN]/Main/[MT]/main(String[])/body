{
  String a, b;
  String foo="foo";
  String bar="bar";
  a=foo.concat(bar).intern();
  b=foo.concat(bar).intern();
  if (a == b && foo != bar) {
    System.out.println("good! " + a);
  }
 else {
    System.out.println("bad!");
  }
}
