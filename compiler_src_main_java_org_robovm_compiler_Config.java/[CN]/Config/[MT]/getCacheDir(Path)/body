{
  File srcRoot=path.getFile();
  if (path.getFile().isFile()) {
    srcRoot=srcRoot.getParentFile();
  }
  try {
    return new File(makeFileRelativeTo(cacheDir,srcRoot.getCanonicalFile()),getArchiveName(path) + ".classes");
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
