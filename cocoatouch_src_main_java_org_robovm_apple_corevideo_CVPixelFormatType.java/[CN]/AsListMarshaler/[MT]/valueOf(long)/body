{
  for (  CVPixelFormatType v : values()) {
    if (v.n == n) {
      return v;
    }
  }
  throw new IllegalArgumentException("No constant with value " + n + " found in "+ CVPixelFormatType.class.getName());
}
