{
  if (customClass) {
    objc_motionCancelledSuper(getSuper(),this,motionCancelled$withEvent$,motion,event);
  }
 else {
    objc_motionCancelled(this,motionCancelled$withEvent$,motion,event);
  }
}
