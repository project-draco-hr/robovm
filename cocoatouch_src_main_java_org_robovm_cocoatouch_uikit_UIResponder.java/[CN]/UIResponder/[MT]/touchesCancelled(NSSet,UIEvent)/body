{
  if (customClass) {
    objc_touchesCancelledSuper(getSuper(),this,touchesCancelled$withEvent$,touches,event);
  }
 else {
    objc_touchesCancelled(this,touchesCancelled$withEvent$,touches,event);
  }
}
