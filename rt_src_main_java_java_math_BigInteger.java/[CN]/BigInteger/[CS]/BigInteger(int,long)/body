{
  this.sign=sign;
  if ((val & 0xFFFFFFFF00000000L) == 0) {
    numberLength=1;
    digits=new int[]{(int)val};
  }
 else {
    numberLength=2;
    digits=new int[]{(int)val,(int)(val >> 32)};
  }
}
