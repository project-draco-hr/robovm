{
  if (firstNonzeroDigit == -2) {
    int i;
    if (this.sign == 0) {
      i=-1;
    }
 else {
      for (i=0; digits[i] == 0; i++) {
      }
    }
    firstNonzeroDigit=i;
  }
  return firstNonzeroDigit;
}
