{
synchronized (lock) {
    if (buf == null) {
      throw new IOException(Messages.getString("luni.24"));
    }
    if (pos < buf.length) {
      return buf[pos++];
    }
    return in.read();
  }
}
