{
  if (customClass) {
    return objc_getBackgroundImageSuper(getSuper(),this,backgroundImageForToolbarPosition$barMetrics$,topOrBottom,barMetrics);
  }
 else {
    return objc_getBackgroundImage(this,backgroundImageForToolbarPosition$barMetrics$,topOrBottom,barMetrics);
  }
}
