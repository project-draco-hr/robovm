{
  if (digesta.length != digestb.length) {
    return false;
  }
  boolean result=true;
  for (int i=0; i < digesta.length; i++) {
    if (digesta[i] != digestb[i]) {
      result=false;
    }
  }
  return result;
}
