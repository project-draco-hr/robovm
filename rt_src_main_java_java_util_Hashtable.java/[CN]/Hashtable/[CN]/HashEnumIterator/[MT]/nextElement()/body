{
  if (isEnumeration) {
    if (hasMoreElements()) {
      Object result=type.get(entry);
      entry=entry.next;
      return (E)result;
    }
    throw new NoSuchElementException();
  }
  return super.next();
}
