{
  Thread spy=new Thread(new Runnable(){
    public void run(){
      while (true) {
        try {
          orphanedMemory(notifyQueue.remove());
        }
 catch (        InterruptedException e) {
        }
      }
    }
  }
);
  spy.setDaemon(true);
  spy.setName("Platform Interface Memory Spy");
  spy.setPriority(Thread.MAX_PRIORITY);
  spy.start();
}
