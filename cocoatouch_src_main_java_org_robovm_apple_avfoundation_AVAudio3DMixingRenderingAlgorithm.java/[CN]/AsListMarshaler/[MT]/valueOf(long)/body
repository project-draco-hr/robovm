{
  for (  AVAudio3DMixingRenderingAlgorithm v : values()) {
    if (v.n == n) {
      return v;
    }
  }
  throw new IllegalArgumentException("No constant with value " + n + " found in "+ AVAudio3DMixingRenderingAlgorithm.class.getName());
}
