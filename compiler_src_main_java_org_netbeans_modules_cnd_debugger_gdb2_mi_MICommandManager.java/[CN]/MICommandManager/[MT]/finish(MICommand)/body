{
  pendingCommands.remove(cmd);
  if (Log.MI.finish) {
    injector.log(String.format("## finished %d\n\r",cmd.getToken()));
    injector.log(String.format("## outstanding: "));
synchronized (pendingCommands) {
      if (pendingCommands.isEmpty()) {
        injector.log(String.format("none"));
      }
 else {
        for (        MICommand oc : pendingCommands) {
          injector.log(String.format(" %d",oc.getToken()));
        }
      }
    }
    injector.log(String.format("\n\r"));
  }
}
