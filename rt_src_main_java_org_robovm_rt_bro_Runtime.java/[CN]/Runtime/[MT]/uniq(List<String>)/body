{
  Set<String> seen=new HashSet<String>();
  List<String> result=new ArrayList<String>();
  for (  String s : l) {
    if (!seen.contains(s)) {
      result.add(s);
      seen.add(s);
    }
  }
  return result;
}
