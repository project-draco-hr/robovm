{
  if (customClass) {
    objc_prepareForCollectionViewUpdatesSuper(getSuper(),this,prepareForCollectionViewUpdates$,updateItems);
  }
 else {
    objc_prepareForCollectionViewUpdates(this,prepareForCollectionViewUpdates$,updateItems);
  }
}
