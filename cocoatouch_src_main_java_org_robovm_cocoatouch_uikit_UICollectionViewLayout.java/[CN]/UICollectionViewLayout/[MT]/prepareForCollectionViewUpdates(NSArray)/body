{
  if (customClass) {
    objc_prepareForCollectionViewUpdatesSuper(getSuper(),prepareForCollectionViewUpdates$,updateItems);
  }
 else {
    objc_prepareForCollectionViewUpdates(this,prepareForCollectionViewUpdates$,updateItems);
  }
}
