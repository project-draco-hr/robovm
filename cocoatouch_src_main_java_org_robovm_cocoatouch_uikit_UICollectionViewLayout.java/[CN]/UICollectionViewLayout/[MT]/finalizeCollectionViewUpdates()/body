{
  if (customClass) {
    objc_finalizeCollectionViewUpdatesSuper(getSuper(),this,finalizeCollectionViewUpdates);
  }
 else {
    objc_finalizeCollectionViewUpdates(this,finalizeCollectionViewUpdates);
  }
}
