{
  if (null == addr || 16 != addr.length) {
    throw new UnknownHostException(Messages.getString("luni.62"));
  }
  if (scope_id < 0) {
    scope_id=0;
  }
  return new Inet6Address(addr,host,scope_id);
}
