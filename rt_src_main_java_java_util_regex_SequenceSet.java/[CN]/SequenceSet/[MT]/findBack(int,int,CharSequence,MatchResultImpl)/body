{
  while (lastIndex >= strIndex) {
    lastIndex=lastIndexOf(testString,strIndex,lastIndex);
    if (lastIndex < 0)     return -1;
    if (next.matches(lastIndex + charCount,testString,matchResult) >= 0)     return lastIndex;
    lastIndex--;
  }
  return -1;
}
