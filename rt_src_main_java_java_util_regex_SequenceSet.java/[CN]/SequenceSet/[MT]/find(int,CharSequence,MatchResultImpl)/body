{
  int strLength=matchResult.getRightBound();
  while (strIndex <= strLength) {
    strIndex=indexOf(testString,strIndex,strLength);
    if (strIndex < 0)     return -1;
    if (next.matches(strIndex + charCount,testString,matchResult) >= 0)     return strIndex;
    strIndex++;
  }
  return -1;
}
