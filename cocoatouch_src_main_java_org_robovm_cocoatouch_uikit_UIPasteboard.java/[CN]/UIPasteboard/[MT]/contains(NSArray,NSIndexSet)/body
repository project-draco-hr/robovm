{
  if (customClass) {
    return objc_containsSuper(getSuper(),this,containsPasteboardTypes$inItemSet$,pasteboardTypes,itemSet);
  }
 else {
    return objc_contains(this,containsPasteboardTypes$inItemSet$,pasteboardTypes,itemSet);
  }
}
