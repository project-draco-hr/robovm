{
  try {
    if (encoded == null) {
      encoded=c.getEncoded(ASN1Encodable.DER);
    }
    return encoded;
  }
 catch (  IOException e) {
    throw new CertificateEncodingException(e.toString());
  }
}
