{
  Var op=pop1("op");
  out.format("    switch i32 %s, label %%%s [\n",op,dflt);
  for (int i=min; i <= max; i++) {
    out.format("        i32 %d, label %%%s\n",i,labels[i - min]);
  }
  out.format("    ]\n");
}
