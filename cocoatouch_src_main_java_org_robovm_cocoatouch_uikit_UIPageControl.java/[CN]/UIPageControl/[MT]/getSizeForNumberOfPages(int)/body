{
  if (customClass) {
    return objc_getSizeForNumberOfPagesSuper(getSuper(),this,sizeForNumberOfPages$,pageCount);
  }
 else {
    return objc_getSizeForNumberOfPages(this,sizeForNumberOfPages$,pageCount);
  }
}
