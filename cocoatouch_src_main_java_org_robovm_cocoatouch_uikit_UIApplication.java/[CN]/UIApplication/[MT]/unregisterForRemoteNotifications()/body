{
  if (customClass) {
    objc_unregisterForRemoteNotificationsSuper(getSuper(),this,unregisterForRemoteNotifications);
  }
 else {
    objc_unregisterForRemoteNotifications(this,unregisterForRemoteNotifications);
  }
}
