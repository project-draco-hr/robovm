{
  if (customClass) {
    objc_setIdleTimerDisabledSuper(getSuper(),this,setIdleTimerDisabled$,idleTimerDisabled);
  }
 else {
    objc_setIdleTimerDisabled(this,setIdleTimerDisabled$,idleTimerDisabled);
  }
}
