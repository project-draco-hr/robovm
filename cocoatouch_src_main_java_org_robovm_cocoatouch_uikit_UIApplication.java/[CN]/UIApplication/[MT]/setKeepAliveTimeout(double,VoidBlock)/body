{
  if (customClass) {
    return objc_setKeepAliveTimeoutSuper(getSuper(),this,setKeepAliveTimeout$handler$,timeout,keepAliveHandler);
  }
 else {
    return objc_setKeepAliveTimeout(this,setKeepAliveTimeout$handler$,timeout,keepAliveHandler);
  }
}
