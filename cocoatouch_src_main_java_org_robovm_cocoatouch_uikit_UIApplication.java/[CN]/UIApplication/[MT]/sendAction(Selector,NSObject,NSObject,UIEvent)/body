{
  if (customClass) {
    return objc_sendActionSuper(getSuper(),sendAction$to$from$forEvent$,action,target,sender,event);
  }
 else {
    return objc_sendAction(this,sendAction$to$from$forEvent$,action,target,sender,event);
  }
}
