{
  if (null == src) {
    throw new NullPointerException(Messages.getString("luni.DD"));
  }
  if (null == charsetName) {
    throw new IllegalArgumentException(Messages.getString("luni.DC"));
  }
  input=Channels.newReader(src,charsetName);
  initialization();
}
