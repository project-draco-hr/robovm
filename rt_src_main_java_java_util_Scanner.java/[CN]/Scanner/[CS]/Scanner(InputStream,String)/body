{
  if (null == src) {
    throw new NullPointerException(Messages.getString("luni.AA"));
  }
  try {
    input=new InputStreamReader(src,charsetName);
  }
 catch (  UnsupportedEncodingException e) {
    throw new IllegalArgumentException(e.getMessage());
  }
  initialization();
}
