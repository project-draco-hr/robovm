{
  boolean gotLine=false;
  if (pos >= CR.length) {
    gotLine=true;
    for (int i=0; i < CR.length; i++) {
      if (byteBufferArray[pos - CR.length + i] != CR[i]) {
        gotLine=false;
        break;
      }
    }
  }
  return gotLine;
}
