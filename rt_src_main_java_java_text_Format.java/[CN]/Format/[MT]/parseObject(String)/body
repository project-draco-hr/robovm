{
  ParsePosition position=new ParsePosition(0);
  Object result=parseObject(string,position);
  if (position.getIndex() == 0) {
    throw new ParseException(Messages.getString("text.1C"),position.getErrorIndex());
  }
  return result;
}
