{
  String optPath="opt";
  if (config.getLlvmBinDir() != null) {
    optPath=new File(config.getLlvmBinDir(),"opt").getAbsolutePath();
  }
  outFile.getParentFile().mkdirs();
  new Executor(config.getLogger(),optPath).args(options,"-o=" + outFile.toString(),inFile).exec();
}
