{
  boolean res=false;
  if (contains(obj)) {
    Unit successor;
    if ((successor=getSuccOf((E)obj)) == null)     successor=getPredOf((E)obj);
    res=innerChain.remove(obj);
    ((E)obj).redirectJumpsToThisTo(successor);
  }
  return res;
}
