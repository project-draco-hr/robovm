{
  if (!state)   throw new IllegalStateException("remove called before first next() call");
  Unit successor;
  if ((successor=getSuccOf(lastObject)) == null)   successor=getPredOf(lastObject);
  innerIterator.remove();
  lastObject.redirectJumpsToThisTo(successor);
}
