{
synchronized (lock) {
    if (isClosed()) {
      throw new IOException(Messages.getString("luni.A7"));
    }
    if (pos >= buf.length) {
      out.write(buf,0,buf.length);
      pos=0;
    }
    buf[pos++]=(char)oneChar;
  }
}
