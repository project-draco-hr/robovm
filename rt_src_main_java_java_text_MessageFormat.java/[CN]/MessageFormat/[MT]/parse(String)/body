{
  ParsePosition position=new ParsePosition(0);
  Object[] result=parse(string,position);
  if (position.getIndex() == 0) {
    throw new ParseException(Messages.getString("text.1B"),position.getErrorIndex());
  }
  return result;
}
