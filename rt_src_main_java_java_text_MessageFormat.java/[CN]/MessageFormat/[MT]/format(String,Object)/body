{
  if (objects != null) {
    for (int i=0; i < objects.length; i++) {
      if (objects[i] == null) {
        objects[i]="null";
      }
    }
  }
  return com.ibm.icu.text.MessageFormat.format(template,objects);
}
