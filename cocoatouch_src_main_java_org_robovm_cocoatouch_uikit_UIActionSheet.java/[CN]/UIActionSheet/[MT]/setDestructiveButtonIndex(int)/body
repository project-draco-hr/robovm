{
  if (customClass) {
    objc_setDestructiveButtonIndexSuper(getSuper(),this,setDestructiveButtonIndex$,destructiveButtonIndex);
  }
 else {
    objc_setDestructiveButtonIndex(this,setDestructiveButtonIndex$,destructiveButtonIndex);
  }
}
