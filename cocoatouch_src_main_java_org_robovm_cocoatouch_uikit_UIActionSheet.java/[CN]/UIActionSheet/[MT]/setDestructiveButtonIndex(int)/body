{
  if (customClass) {
    objc_setDestructiveButtonIndexSuper(getSuper(),setDestructiveButtonIndex$,destructiveButtonIndex);
  }
 else {
    objc_setDestructiveButtonIndex(this,setDestructiveButtonIndex$,destructiveButtonIndex);
  }
}
