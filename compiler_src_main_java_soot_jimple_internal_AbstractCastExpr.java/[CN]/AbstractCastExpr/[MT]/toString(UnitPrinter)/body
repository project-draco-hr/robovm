{
  up.literal("(");
  up.type(type);
  up.literal(") ");
  if (PrecedenceTest.needsBrackets(opBox,this))   up.literal("(");
  opBox.toString(up);
  if (PrecedenceTest.needsBrackets(opBox,this))   up.literal(")");
}
