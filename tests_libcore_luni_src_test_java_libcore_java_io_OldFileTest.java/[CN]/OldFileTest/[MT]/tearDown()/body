{
  if (tempFile.exists() && !tempFile.delete())   System.out.println("OldFileTest.tearDown() failed, could not delete file!");
  if (!tempDirectory.delete())   System.out.println("OldFileTest.tearDown() failed, could not delete directory!");
}
