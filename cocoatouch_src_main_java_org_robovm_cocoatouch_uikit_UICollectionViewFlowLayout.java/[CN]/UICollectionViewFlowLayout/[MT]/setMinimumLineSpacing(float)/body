{
  if (customClass) {
    objc_setMinimumLineSpacingSuper(getSuper(),setMinimumLineSpacing$,minimumLineSpacing);
  }
 else {
    objc_setMinimumLineSpacing(this,setMinimumLineSpacing$,minimumLineSpacing);
  }
}
