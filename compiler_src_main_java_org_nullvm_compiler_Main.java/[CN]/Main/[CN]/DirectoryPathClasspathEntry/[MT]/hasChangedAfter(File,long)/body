{
  for (  File f : dir.listFiles()) {
    if (f.isFile()) {
      if (f.lastModified() > timestamp) {
        return true;
      }
    }
 else {
      if (hasChangedAfter(f,timestamp)) {
        return true;
      }
    }
  }
  return false;
}
