{
  if (llvmCacheDir == null) {
    try {
      llvmCacheDir=new File(makeFileRelativeTo(llvmCache,path.getFile().getCanonicalFile().getParentFile()),path.getFile().getName() + ".classes");
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
  return llvmCacheDir;
}
