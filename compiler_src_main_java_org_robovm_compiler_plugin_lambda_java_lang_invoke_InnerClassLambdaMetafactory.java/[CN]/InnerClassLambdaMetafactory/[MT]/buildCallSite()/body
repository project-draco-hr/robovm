{
  byte[] classData=spinInnerClass();
  if (invokedType.parameterCount() == 0) {
    return new SCallSite(lambdaClassName,classData,"<init>",invokedType);
  }
 else {
    return new SCallSite(lambdaClassName,classData,NAME_FACTORY,invokedType);
  }
}
