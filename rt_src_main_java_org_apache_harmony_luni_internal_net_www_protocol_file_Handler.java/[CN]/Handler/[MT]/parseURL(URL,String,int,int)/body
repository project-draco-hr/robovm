{
  if (end < start) {
    return;
  }
  String parseString="";
  if (start < end) {
    parseString=str.substring(start,end).replace('\\','/');
  }
  super.parseURL(u,parseString,0,parseString.length());
}
