{
  assertEquals("Test 1: Returned incorrect number of available bytes;",text.length() / 2,lnis.available());
  lnis.close();
  try {
    lnis.available();
    fail("Test 2: IOException expected.");
  }
 catch (  IOException e) {
  }
}
