{
  lnis.setLineNumber(42);
  assertEquals("Test 1: Failed to set line number;",42,lnis.getLineNumber());
  lnis.setLineNumber(89);
  assertEquals("Test 2: Failed to set line number;",89,lnis.getLineNumber());
}
