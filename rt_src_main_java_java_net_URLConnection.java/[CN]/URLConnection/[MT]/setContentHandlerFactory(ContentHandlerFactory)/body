{
  if (contentHandlerFactory != null) {
    throw new Error(Messages.getString("luni.98"));
  }
  SecurityManager sManager=System.getSecurityManager();
  if (sManager != null) {
    sManager.checkSetFactory();
  }
  contentHandlerFactory=contentFactory;
}
