{
  if (customClass) {
    objc_setVideoMaximumDurationSuper(getSuper(),setVideoMaximumDuration$,videoMaximumDuration);
  }
 else {
    objc_setVideoMaximumDuration(this,setVideoMaximumDuration$,videoMaximumDuration);
  }
}
