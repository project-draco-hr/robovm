{
  if (customClass) {
    objc_setVideoMaximumDurationSuper(getSuper(),this,setVideoMaximumDuration$,videoMaximumDuration);
  }
 else {
    objc_setVideoMaximumDuration(this,setVideoMaximumDuration$,videoMaximumDuration);
  }
}
