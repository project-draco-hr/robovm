{
  super.init(config);
  os=config.getOs();
  if (os == null) {
    os=OS.getDefaultOS(config.getLlvmHomeDir());
  }
  arch=config.getArch();
  if (arch == null) {
    arch=Arch.getDefaultArch(config.getLlvmHomeDir());
  }
}
