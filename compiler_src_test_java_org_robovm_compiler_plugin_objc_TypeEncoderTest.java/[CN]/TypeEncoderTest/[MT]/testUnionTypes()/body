{
  assertEquals("v(?=i{?=ff})",new TypeEncoder().encode(toSootClass(Methods.class).getMethodByName("m6"),false));
  assertEquals("v(?=i{?=dd})",new TypeEncoder().encode(toSootClass(Methods.class).getMethodByName("m6"),true));
}
