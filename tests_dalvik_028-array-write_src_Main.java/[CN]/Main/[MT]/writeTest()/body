{
  long start, end;
  writeArray(0);
  System.out.println("Running writeTest...");
  start=System.nanoTime();
  for (int i=1; i < 20; i++)   writeArray(i);
  end=System.nanoTime();
  report(start,end);
}
