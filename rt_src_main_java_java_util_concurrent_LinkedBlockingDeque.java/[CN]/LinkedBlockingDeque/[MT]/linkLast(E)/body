{
  if (count >= capacity)   return false;
  Node<E> l=last;
  Node<E> x=new Node<E>(e,l,null);
  last=x;
  if (first == null)   first=x;
 else   l.next=x;
  ++count;
  notEmpty.signal();
  return true;
}
