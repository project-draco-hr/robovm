{
  int where=offsets[name];
  int len=n2h.get2u(bytes,where);
  if (len != s.length())   return false;
  for (int i=0; i < len; i++) {
    if (bytes[i + where + 2] != s.charAt(i))     return false;
  }
  return true;
}
