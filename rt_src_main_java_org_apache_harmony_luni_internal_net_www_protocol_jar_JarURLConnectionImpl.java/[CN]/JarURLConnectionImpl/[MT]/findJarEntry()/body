{
  String entryName=getEntryName();
  if (entryName == null) {
    return;
  }
  String decodedName=URLDecoder.decode(entryName,"UTF-8");
  jarEntry=jarFile.getJarEntry(decodedName);
  if (jarEntry == null) {
    throw new FileNotFoundException(entryName);
  }
}
