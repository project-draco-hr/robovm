{
  if (sslContext == null)   throw new NullPointerException();
  SecurityManager securityManager=System.getSecurityManager();
  if (securityManager != null)   securityManager.checkPermission(new SSLPermission("setDefaultSSLContext"));
  defaultSSLContext=sslContext;
}
