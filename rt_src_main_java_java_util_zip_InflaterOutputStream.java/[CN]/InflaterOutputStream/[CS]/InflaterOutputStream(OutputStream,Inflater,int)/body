{
  super(out);
  if (null == out || null == infl) {
    throw new NullPointerException();
  }
  if (bufLen <= 0) {
    throw new IllegalArgumentException();
  }
  inf=infl;
  buf=new byte[bufLen];
}
