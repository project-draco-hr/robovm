{
  assert(xar instanceof Object);
  assert(!(xar instanceof Classes));
  assert(xar instanceof Classes[]);
  assert(!(xar instanceof MoreClasses[]));
  assert(xar instanceof Object[]);
  assert(!(xar instanceof Object[][]));
}
