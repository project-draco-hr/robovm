{
  for (  ABGroupProperty v : values) {
    if (v.value() == value) {
      return v;
    }
  }
  throw new IllegalArgumentException("No constant with value " + value + " found in "+ ABGroupProperty.class.getName());
}
