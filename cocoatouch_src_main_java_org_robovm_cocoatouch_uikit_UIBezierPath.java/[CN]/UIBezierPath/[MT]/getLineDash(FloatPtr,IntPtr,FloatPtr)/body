{
  if (customClass) {
    objc_getLineDashSuper(getSuper(),this,getLineDash$count$phase$,pattern,count,phase);
  }
 else {
    objc_getLineDash(this,getLineDash$count$phase$,pattern,count,phase);
  }
}
