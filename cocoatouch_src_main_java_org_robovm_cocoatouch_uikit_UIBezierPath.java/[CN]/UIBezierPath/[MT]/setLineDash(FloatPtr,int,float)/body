{
  if (customClass) {
    objc_setLineDashSuper(getSuper(),this,setLineDash$count$phase$,pattern,count,phase);
  }
 else {
    objc_setLineDash(this,setLineDash$count$phase$,pattern,count,phase);
  }
}
