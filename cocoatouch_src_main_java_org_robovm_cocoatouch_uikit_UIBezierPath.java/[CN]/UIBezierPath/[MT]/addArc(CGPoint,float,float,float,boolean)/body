{
  if (customClass) {
    objc_addArcSuper(getSuper(),this,addArcWithCenter$radius$startAngle$endAngle$clockwise$,center,radius,startAngle,endAngle,clockwise);
  }
 else {
    objc_addArc(this,addArcWithCenter$radius$startAngle$endAngle$clockwise$,center,radius,startAngle,endAngle,clockwise);
  }
}
