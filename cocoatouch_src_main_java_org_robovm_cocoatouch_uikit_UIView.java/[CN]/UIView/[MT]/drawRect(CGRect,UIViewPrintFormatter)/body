{
  if (customClass) {
    objc_drawRectSuper(getSuper(),this,drawRect$forViewPrintFormatter$,area,formatter);
  }
 else {
    objc_drawRect(this,drawRect$forViewPrintFormatter$,area,formatter);
  }
}
