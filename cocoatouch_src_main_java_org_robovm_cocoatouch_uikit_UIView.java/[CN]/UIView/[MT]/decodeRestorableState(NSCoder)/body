{
  if (customClass) {
    objc_decodeRestorableStateSuper(getSuper(),decodeRestorableStateWithCoder$,coder);
  }
 else {
    objc_decodeRestorableState(this,decodeRestorableStateWithCoder$,coder);
  }
}
