{
  if (customClass) {
    objc_exchangeSubviewSuper(getSuper(),this,exchangeSubviewAtIndex$withSubviewAtIndex$,index1,index2);
  }
 else {
    objc_exchangeSubview(this,exchangeSubviewAtIndex$withSubviewAtIndex$,index1,index2);
  }
}
