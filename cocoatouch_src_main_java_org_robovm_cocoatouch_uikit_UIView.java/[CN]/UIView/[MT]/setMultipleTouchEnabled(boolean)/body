{
  if (customClass) {
    objc_setMultipleTouchEnabledSuper(getSuper(),this,setMultipleTouchEnabled$,multipleTouchEnabled);
  }
 else {
    objc_setMultipleTouchEnabled(this,setMultipleTouchEnabled$,multipleTouchEnabled);
  }
}
