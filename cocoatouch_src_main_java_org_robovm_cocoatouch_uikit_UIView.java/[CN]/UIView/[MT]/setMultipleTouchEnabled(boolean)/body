{
  if (customClass) {
    objc_setMultipleTouchEnabledSuper(getSuper(),setMultipleTouchEnabled$,multipleTouchEnabled);
  }
 else {
    objc_setMultipleTouchEnabled(this,setMultipleTouchEnabled$,multipleTouchEnabled);
  }
}
