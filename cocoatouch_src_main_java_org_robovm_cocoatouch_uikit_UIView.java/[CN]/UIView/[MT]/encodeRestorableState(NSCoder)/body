{
  if (customClass) {
    objc_encodeRestorableStateSuper(getSuper(),this,encodeRestorableStateWithCoder$,coder);
  }
 else {
    objc_encodeRestorableState(this,encodeRestorableStateWithCoder$,coder);
  }
}
