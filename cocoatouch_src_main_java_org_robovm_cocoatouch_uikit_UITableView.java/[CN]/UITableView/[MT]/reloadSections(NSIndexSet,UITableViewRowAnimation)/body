{
  if (customClass) {
    objc_reloadSectionsSuper(getSuper(),reloadSections$withRowAnimation$,sections,animation);
  }
 else {
    objc_reloadSections(this,reloadSections$withRowAnimation$,sections,animation);
  }
}
