{
  if (customClass) {
    return objc_getBackgroundImageSuper(getSuper(),this,backgroundImageForState$barMetrics$,state,barMetrics);
  }
 else {
    return objc_getBackgroundImage(this,backgroundImageForState$barMetrics$,state,barMetrics);
  }
}
