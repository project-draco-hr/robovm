{
  cp_info c;
  int i;
  for (i=1; i < constant_pool_count; i++) {
    c=constant_pool[i];
    G.v().out.println("[" + i + "] "+ c.typeName()+ "="+ c.toString(constant_pool));
    if ((constant_pool[i]).tag == cp_info.CONSTANT_Long || (constant_pool[i]).tag == cp_info.CONSTANT_Double) {
      i++;
    }
  }
}
