{
  sb.append(prefix).append("Unparseable extension value:\n");
  if (encoding == null) {
    encoding=getEncoded();
  }
  if (encoding == null) {
    sb.append("NULL\n");
  }
 else {
    sb.append(Array.toString(encoding,prefix));
  }
}
