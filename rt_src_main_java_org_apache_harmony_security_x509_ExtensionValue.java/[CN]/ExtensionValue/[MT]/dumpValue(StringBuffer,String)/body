{
  buffer.append(prefix).append("Unparseable extension value:\n");
  if (encoding == null) {
    encoding=getEncoded();
  }
  if (encoding == null) {
    buffer.append("NULL\n");
  }
 else {
    buffer.append(Array.toString(encoding,prefix));
  }
}
