{
  if (index > size || index < 0)   throw new ClassFormatError("constant pool index out of range");
  if (tags[index] != tag)   throw new ClassFormatError("did not find " + CONSTANT_Name[tag] + " value at pool index "+ index+ ", found: "+ CONSTANT_Name[tags[index]]);
}
