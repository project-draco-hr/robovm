{
  if (customClass) {
    return objc_getPositionSuper(getSuper(),positionFromPosition$toBoundary$inDirection$,position,granularity,direction);
  }
 else {
    return objc_getPosition(this,positionFromPosition$toBoundary$inDirection$,position,granularity,direction);
  }
}
