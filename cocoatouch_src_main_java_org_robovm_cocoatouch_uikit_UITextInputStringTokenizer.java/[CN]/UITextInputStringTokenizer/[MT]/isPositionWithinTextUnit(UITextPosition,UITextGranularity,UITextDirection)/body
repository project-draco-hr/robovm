{
  if (customClass) {
    return objc_isPositionWithinTextUnitSuper(getSuper(),this,isPosition$withinTextUnit$inDirection$,position,granularity,direction);
  }
 else {
    return objc_isPositionWithinTextUnit(this,isPosition$withinTextUnit$inDirection$,position,granularity,direction);
  }
}
