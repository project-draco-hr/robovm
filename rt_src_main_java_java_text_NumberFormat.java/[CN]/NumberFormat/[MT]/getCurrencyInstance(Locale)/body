{
  com.ibm.icu.text.DecimalFormat icuFormat=(com.ibm.icu.text.DecimalFormat)com.ibm.icu.text.NumberFormat.getCurrencyInstance(locale);
  String pattern=icuFormat.toPattern();
  return new DecimalFormat(pattern,new DecimalFormatSymbols(locale));
}
