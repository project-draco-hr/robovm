{
  for (  ABPersonKind v : values) {
    if (v.value().intValue() == value.intValue()) {
      return v;
    }
  }
  throw new IllegalArgumentException("No constant with value " + value + " found in "+ ABPersonKind.class.getName());
}
