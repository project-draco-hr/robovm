{
  File folder=null;
  try {
    folder=File.createTempFile("hyts_resources","",null);
    folder.delete();
    folder.mkdirs();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  folder.deleteOnExit();
  return folder;
}
