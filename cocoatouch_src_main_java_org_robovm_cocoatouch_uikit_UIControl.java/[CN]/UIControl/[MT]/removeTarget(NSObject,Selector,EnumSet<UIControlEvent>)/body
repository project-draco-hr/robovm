{
  if (customClass) {
    objc_removeTargetSuper(getSuper(),this,removeTarget$action$forControlEvents$,target,action,controlEvents);
  }
 else {
    objc_removeTarget(this,removeTarget$action$forControlEvents$,target,action,controlEvents);
  }
}
