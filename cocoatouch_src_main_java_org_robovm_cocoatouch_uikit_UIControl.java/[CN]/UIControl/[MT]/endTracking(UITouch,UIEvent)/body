{
  if (customClass) {
    objc_endTrackingSuper(getSuper(),this,endTrackingWithTouch$withEvent$,touch,event);
  }
 else {
    objc_endTracking(this,endTrackingWithTouch$withEvent$,touch,event);
  }
}
