{
  if (customClass) {
    objc_addTargetSuper(getSuper(),addTarget$action$forControlEvents$,target,action,controlEvents);
  }
 else {
    objc_addTarget(this,addTarget$action$forControlEvents$,target,action,controlEvents);
  }
}
