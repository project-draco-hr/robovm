{
  if (customClass) {
    objc_removeTargetSuper(getSuper(),removeTarget$action$forControlEvents$,target,action,controlEvents);
  }
 else {
    objc_removeTarget(this,removeTarget$action$forControlEvents$,target,action,controlEvents);
  }
}
