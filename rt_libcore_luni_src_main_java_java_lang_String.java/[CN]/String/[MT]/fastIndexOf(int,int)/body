{
  int _count=count;
  int _offset=offset;
  char[] _value=value;
  if (start < _count) {
    if (start < 0) {
      start=0;
    }
    for (int i=_offset + start; i < _offset + _count; i++) {
      if (_value[i] == c) {
        return i - _offset;
      }
    }
  }
  return -1;
}
