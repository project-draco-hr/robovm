{
  if (customClass) {
    return objc_getClosestPositionSuper(getSuper(),this,closestPositionToPoint$withinRange$,point,range);
  }
 else {
    return objc_getClosestPosition(this,closestPositionToPoint$withinRange$,point,range);
  }
}
