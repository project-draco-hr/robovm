{
  if (customClass) {
    objc_setSelectedTextRangeSuper(getSuper(),this,setSelectedTextRange$,selectedTextRange);
  }
 else {
    objc_setSelectedTextRange(this,setSelectedTextRange$,selectedTextRange);
  }
}
