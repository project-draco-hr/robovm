{
  if (customClass) {
    return objc_getPositionSuper(getSuper(),positionWithinRange$farthestInDirection$,range,direction);
  }
 else {
    return objc_getPosition(this,positionWithinRange$farthestInDirection$,range,direction);
  }
}
