{
  if (customClass) {
    return objc_shouldChangeTextSuper(getSuper(),this,shouldChangeTextInRange$replacementText$,range,text);
  }
 else {
    return objc_shouldChangeText(this,shouldChangeTextInRange$replacementText$,range,text);
  }
}
