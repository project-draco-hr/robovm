{
  if (customClass) {
    objc_setBaseWritingDirectionSuper(getSuper(),setBaseWritingDirection$forRange$,writingDirection,range);
  }
 else {
    objc_setBaseWritingDirection(this,setBaseWritingDirection$forRange$,writingDirection,range);
  }
}
