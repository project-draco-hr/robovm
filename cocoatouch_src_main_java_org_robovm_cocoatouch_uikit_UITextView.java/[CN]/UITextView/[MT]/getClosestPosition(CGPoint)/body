{
  if (customClass) {
    return objc_getClosestPositionSuper(getSuper(),this,closestPositionToPoint$,point);
  }
 else {
    return objc_getClosestPosition(this,closestPositionToPoint$,point);
  }
}
