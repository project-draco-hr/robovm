{
  if (customClass) {
    return objc_getTextRangeSuper(getSuper(),textRangeFromPosition$toPosition$,fromPosition,toPosition);
  }
 else {
    return objc_getTextRange(this,textRangeFromPosition$toPosition$,fromPosition,toPosition);
  }
}
