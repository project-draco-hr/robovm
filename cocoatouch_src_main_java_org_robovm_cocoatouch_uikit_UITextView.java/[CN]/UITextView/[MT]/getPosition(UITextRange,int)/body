{
  if (customClass) {
    return objc_getPositionSuper(getSuper(),this,positionWithinRange$atCharacterOffset$,range,offset);
  }
 else {
    return objc_getPosition(this,positionWithinRange$atCharacterOffset$,range,offset);
  }
}
