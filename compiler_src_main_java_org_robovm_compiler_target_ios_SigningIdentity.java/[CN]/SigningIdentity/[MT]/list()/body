{
  try {
    return parse(new Executor(Logger.NULL_LOGGER,"security").args("find-identity","-v","-p","codesigning").execCapture());
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
