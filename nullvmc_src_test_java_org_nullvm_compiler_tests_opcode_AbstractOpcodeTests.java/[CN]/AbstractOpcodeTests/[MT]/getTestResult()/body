{
  out.format("\nTest result:\n");
  out.format("%d tests in total\n",totalTests);
  out.format("%d tests failed\n",failedTests);
  if (failedTests == 0) {
    out.format("All tests were successful!\n");
  }
  out.flush();
  return sw.toString();
}
