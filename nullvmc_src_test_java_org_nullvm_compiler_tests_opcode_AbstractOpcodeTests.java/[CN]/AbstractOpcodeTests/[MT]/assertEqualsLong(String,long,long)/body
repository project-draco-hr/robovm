{
  totalTests++;
  if (expected != actual) {
    failedTests++;
    out.format("%d: ****** Test '%s' failed: %d != %d\n",totalTests,test,expected,actual);
  }
 else {
    out.format("%d: Test '%s' succeeded: %d == %d\n",totalTests,test,expected,actual);
  }
}
