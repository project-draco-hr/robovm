{
  totalTests++;
  if (o != null) {
    failedTests++;
    out.format("%d: ****** Test '%s' failed: Object != null\n",totalTests,test);
  }
 else {
    out.format("%d: Test '%s' succeeded: Object == null\n",totalTests,test);
  }
}
