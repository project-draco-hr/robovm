{
  totalTests++;
  if (expected != actual) {
    failedTests++;
    out.format("%d: ****** Test '%s' failed: o1 != o2\n",totalTests,test);
  }
 else {
    out.format("%d: Test '%s' succeeded: o1 == o2\n",totalTests,test);
  }
}
