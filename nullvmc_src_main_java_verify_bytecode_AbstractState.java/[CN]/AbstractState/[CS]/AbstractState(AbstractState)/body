{
  pc=-1;
  sp=state.sp;
  constructor_start=state.constructor_start;
  context=state.context;
  jsrStack=state.jsrStack;
  storage=new Type[state.storage.length];
  System.arraycopy(state.storage,0,storage,0,state.storage.length);
}
