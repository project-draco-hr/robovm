{
  if (sp + 1 >= max_stack()) {
    throw new verify.VerificationException("stack overflow: push(" + type1 + ", "+ type2+ ")");
  }
  Type[] wstate=storage;
  wstate[sp++]=type1;
  wstate[sp++]=type2;
}
