{
  if (sp < 1) {
    throw new verify.VerificationException("stack underflow: pop()");
  }
  Type type=(storage)[--sp];
  if (type.storageClass != storage_class) {
    throw new IncompatibleTypesException("unexpected value on stack: pop()",type,storage_class);
  }
  return type;
}
