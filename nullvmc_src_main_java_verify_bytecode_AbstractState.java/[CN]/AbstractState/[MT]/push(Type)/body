{
  if (type == null)   throw new InternalError("pushing null value!");
  if (sp >= max_stack()) {
    throw new verify.VerificationException("stack overflow: push(" + type + ")");
  }
  (storage)[sp++]=type;
}
