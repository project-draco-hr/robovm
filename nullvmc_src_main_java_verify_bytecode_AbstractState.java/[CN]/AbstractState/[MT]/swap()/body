{
  Type[] wstate=storage;
  if (sp < 2)   throw new verify.VerificationException("stack underflow in swap() operation");
  Type tmp=wstate[sp - 1];
  wstate[sp - 1]=wstate[sp - 2];
  wstate[sp - 2]=tmp;
}
