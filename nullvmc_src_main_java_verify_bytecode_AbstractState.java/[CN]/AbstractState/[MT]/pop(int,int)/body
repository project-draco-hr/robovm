{
  if (sp < 2) {
    throw new verify.VerificationException("stack underflow: pop()");
  }
  Type rstate[]=storage;
  Type type1=rstate[--sp];
  Type type0=rstate[--sp];
  if (type1.storageClass != sc1 || type0.storageClass != sc0) {
    throw new IncompatibleTypesException("unexpected value on stack",type0,sc0);
  }
}
