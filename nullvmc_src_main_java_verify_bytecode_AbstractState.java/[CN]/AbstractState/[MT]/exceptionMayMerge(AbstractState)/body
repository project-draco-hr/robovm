{
  Type[] storage=this.storage;
  Type[] other_storage=state.storage;
  int max=storage.length;
  for (int i=max_stack(); i < max; i++) {
    Type this_type=storage[i];
    Type other_type=other_storage[i];
    if (this_type == other_type)     continue;
    if (this_type == null)     continue;
    if (other_type == null)     return true;
    if (this_type.storageClass == SC_INT && other_type.storageClass == SC_INT)     continue;
    may_merge_true+=1;
    return true;
  }
  may_merge_false+=1;
  return false;
}
