{
  Clazz clazz=cache.get(internalName);
  if (clazz == null) {
    for (    Path p : paths) {
      clazz=p.loadGeneratedClass(internalName);
      if (clazz != null) {
        break;
      }
    }
  }
  return clazz;
}
