{
  if (pattern.equals("")) {
    throw new IllegalArgumentException(Messages.getString("logging.19"));
  }
  init(pattern,Boolean.valueOf(append),Integer.valueOf(DEFAULT_LIMIT),Integer.valueOf(DEFAULT_COUNT));
}
