{
  if (cacheDir == null) {
    try {
      cacheDir=new File(makeFileRelativeTo(cache,path.getFile().getCanonicalFile().getParentFile()),path.getFile().getName() + ".classes");
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
  return cacheDir;
}
