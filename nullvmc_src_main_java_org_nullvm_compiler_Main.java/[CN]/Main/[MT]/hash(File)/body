{
  InputStream in=null;
  try {
    in=new BufferedInputStream(new FileInputStream(f));
    return hash(in);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    IOUtils.closeQuietly(in);
  }
}
