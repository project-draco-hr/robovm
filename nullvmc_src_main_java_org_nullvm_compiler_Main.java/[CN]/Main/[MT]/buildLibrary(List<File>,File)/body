{
  if (verbose) {
    stdout.format("Building library '%s'\n",libFile);
  }
  String arPath="ar";
  if (arBin != null) {
    arPath=arBin.getAbsolutePath();
  }
  if (libFile.exists()) {
    libFile.delete();
  }
  exec(arPath,"rcs",libFile,files);
  return libFile;
}
