{
  Object[] array=new Float[10][];
  Object o=new Nonce[1];
  try {
    array[0]=o;
    fail();
  }
 catch (  ArrayStoreException ex) {
    assertEquals("libcore.java.lang.ArrayStoreExceptionTest$Nonce[] cannot be stored " + "in an array of type java.lang.Float[][]",ex.getMessage());
  }
}
