{
  String[] src=new String[1];
  int[] dest=new int[1];
  try {
    System.arraycopy(src,0,dest,0,1);
  }
 catch (  ArrayStoreException ex) {
    assertEquals("java.lang.String[] and int[] are incompatible array types",ex.getMessage());
  }
}
