{
  if (customClass) {
    objc_setDrawableDepthFormatSuper(getSuper(),setDrawableDepthFormat$,drawableDepthFormat);
  }
 else {
    objc_setDrawableDepthFormat(this,setDrawableDepthFormat$,drawableDepthFormat);
  }
}
