{
  if (customClass) {
    objc_setDrawableMultisampleSuper(getSuper(),setDrawableMultisample$,drawableMultisample);
  }
 else {
    objc_setDrawableMultisample(this,setDrawableMultisample$,drawableMultisample);
  }
}
