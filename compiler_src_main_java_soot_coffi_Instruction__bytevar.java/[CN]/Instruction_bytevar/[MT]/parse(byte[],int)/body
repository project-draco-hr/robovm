{
  int indexbyte1=(bc[index]) & 0xff;
  if (isWide) {
    int indexbyte2=(bc[index + 1]) & 0xff;
    arg_b=(indexbyte1 << 8) | indexbyte2;
    return index + 2;
  }
 else {
    arg_b=indexbyte1;
    return index + 1;
  }
}
