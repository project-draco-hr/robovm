{
  Typing r=null;
  count[0]=-1;
  boolean setR=false;
  for (  Typing tg : sigma) {
    int n=this.insertCasts(tg,h,true);
    if (count[0] == -1 || n < count[0]) {
      count[0]=n;
      r=tg;
      setR=true;
    }
  }
  if (setR)   return r;
 else   return null;
}
