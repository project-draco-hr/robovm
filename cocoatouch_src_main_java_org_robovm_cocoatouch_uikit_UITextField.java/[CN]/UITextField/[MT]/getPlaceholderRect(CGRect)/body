{
  if (customClass) {
    return objc_getPlaceholderRectSuper(getSuper(),this,placeholderRectForBounds$,bounds);
  }
 else {
    return objc_getPlaceholderRect(this,placeholderRectForBounds$,bounds);
  }
}
