{
  if (customClass) {
    return objc_getPositionSuper(getSuper(),positionFromPosition$inDirection$offset$,position,direction,offset);
  }
 else {
    return objc_getPosition(this,positionFromPosition$inDirection$offset$,position,direction,offset);
  }
}
