{
  if (customClass) {
    objc_setSelectedTextRangeSuper(getSuper(),setSelectedTextRange$,selectedTextRange);
  }
 else {
    objc_setSelectedTextRange(this,setSelectedTextRange$,selectedTextRange);
  }
}
