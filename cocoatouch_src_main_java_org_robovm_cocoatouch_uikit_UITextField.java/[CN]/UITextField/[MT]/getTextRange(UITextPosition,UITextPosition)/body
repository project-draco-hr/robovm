{
  if (customClass) {
    return objc_getTextRangeSuper(getSuper(),this,textRangeFromPosition$toPosition$,fromPosition,toPosition);
  }
 else {
    return objc_getTextRange(this,textRangeFromPosition$toPosition$,fromPosition,toPosition);
  }
}
