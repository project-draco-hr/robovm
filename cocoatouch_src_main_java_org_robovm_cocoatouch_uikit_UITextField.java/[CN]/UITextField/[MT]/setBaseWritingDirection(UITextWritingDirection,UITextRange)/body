{
  if (customClass) {
    objc_setBaseWritingDirectionSuper(getSuper(),this,setBaseWritingDirection$forRange$,writingDirection,range);
  }
 else {
    objc_setBaseWritingDirection(this,setBaseWritingDirection$forRange$,writingDirection,range);
  }
}
