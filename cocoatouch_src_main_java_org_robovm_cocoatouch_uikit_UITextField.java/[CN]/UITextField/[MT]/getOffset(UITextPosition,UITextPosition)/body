{
  if (customClass) {
    return objc_getOffsetSuper(getSuper(),this,offsetFromPosition$toPosition$,fromPosition,toPosition);
  }
 else {
    return objc_getOffset(this,offsetFromPosition$toPosition$,fromPosition,toPosition);
  }
}
