{
  if (customClass) {
    return objc_clearButtonRectSuper(getSuper(),this,clearButtonRectForBounds$,bounds);
  }
 else {
    return objc_clearButtonRect(this,clearButtonRectForBounds$,bounds);
  }
}
