{
  if (customClass) {
    objc_setMarkedTextSuper(getSuper(),this,setMarkedText$selectedRange$,markedText,selectedRange);
  }
 else {
    objc_setMarkedText(this,setMarkedText$selectedRange$,markedText,selectedRange);
  }
}
