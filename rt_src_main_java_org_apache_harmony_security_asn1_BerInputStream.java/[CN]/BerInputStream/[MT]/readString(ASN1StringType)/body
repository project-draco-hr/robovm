{
  if (tag == type.id) {
    readContent();
  }
 else   if (tag == type.constrId) {
    throw new ASN1Exception(Messages.getString("security.139"));
  }
 else {
    throw new ASN1Exception(Messages.getString("security.13A",tagOffset,Integer.toHexString(tag)));
  }
}
