{
  if (tag != ASN1Constants.TAG_BOOLEAN) {
    throw new ASN1Exception(Messages.getString("security.11C",tagOffset,Integer.toHexString(tag)));
  }
  if (length != 1) {
    throw new ASN1Exception(Messages.getString("security.11D",tagOffset));
  }
  readContent();
}
