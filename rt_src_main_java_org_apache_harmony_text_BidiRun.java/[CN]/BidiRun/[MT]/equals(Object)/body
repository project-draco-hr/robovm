{
  if (o == null) {
    return false;
  }
  if (!(o instanceof BidiRun)) {
    return false;
  }
  BidiRun other=(BidiRun)o;
  return start == other.start && limit == other.limit && level == other.level;
}
