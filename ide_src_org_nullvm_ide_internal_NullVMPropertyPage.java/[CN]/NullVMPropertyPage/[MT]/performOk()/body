{
  try {
    projectProperties.storePreferences(project);
  }
 catch (  BackingStoreException e) {
    MessageDialog.openError(NullVMPlugin.getShell(),"Failed to store properties",e.getMessage());
    return false;
  }
  return true;
}
