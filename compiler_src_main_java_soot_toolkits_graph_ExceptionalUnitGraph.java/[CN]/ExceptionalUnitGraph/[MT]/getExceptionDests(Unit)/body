{
  Collection<ExceptionDest> result=(Collection<ExceptionDest>)unitToExceptionDests.get(u);
  if (result == null) {
    result=new LinkedList<ExceptionDest>();
    result.add(new ExceptionDest(null,throwAnalysis.mightThrow((Unit)u)));
  }
  return result;
}
