{
  for (  Resource res : config.getResources()) {
    res.walk(new Walker(){
      @Override public void process(      Resource resource,      File file,      File destDir) throws IOException {
        copyFile(resource,file,destDir);
      }
    }
,destDir);
  }
}
