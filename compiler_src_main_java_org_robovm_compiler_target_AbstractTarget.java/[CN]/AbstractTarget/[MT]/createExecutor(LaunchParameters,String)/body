{
  Map<String,String> env=launchParameters.getEnvironment();
  return new Executor(config.getLogger(),cmd).args(launchParameters.getArguments()).wd(launchParameters.getWorkingDirectory()).inheritEnv(env == null).env(env == null ? Collections.<String,String>emptyMap() : env);
}
