{
  try {
    X500Principal principal=new X500Principal(subject);
    return createCertificate(publicKey,privateKey,principal,principal,0,true,new Vector<GeneralName>(),new Vector<GeneralSubtree>(),new Vector<GeneralSubtree>());
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}
