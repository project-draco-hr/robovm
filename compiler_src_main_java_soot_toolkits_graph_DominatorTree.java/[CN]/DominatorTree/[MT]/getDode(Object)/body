{
  DominatorNode dode=(DominatorNode)godeToDode.get(gode);
  if (dode == null)   throw new RuntimeException("Assertion failed: Dominator tree does not have a corresponding dode for gode (" + gode + ")");
  return dode;
}
