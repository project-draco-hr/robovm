{
  FunctionType ft=getCallbackFunctionType(dataLayout,method);
  return new FunctionBuilder(method).type(new FunctionType(returnType,ft.isVarargs(),ft.getParameterTypes())).suffix("_callback").linkage(external).attribs(noinline,optsize).build();
}
