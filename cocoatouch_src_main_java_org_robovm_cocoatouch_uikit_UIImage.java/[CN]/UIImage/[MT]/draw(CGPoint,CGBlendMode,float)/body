{
  if (customClass) {
    objc_drawSuper(getSuper(),this,drawAtPoint$blendMode$alpha$,point,blendMode,alpha);
  }
 else {
    objc_draw(this,drawAtPoint$blendMode$alpha$,point,blendMode,alpha);
  }
}
