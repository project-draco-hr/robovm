{
  try {
    if (!connected) {
      connect();
    }
  }
 catch (  IOException e) {
  }
  if (index > -1 && index < header.size()) {
    return header.keySet().toArray(new String[0])[index];
  }
  return null;
}
