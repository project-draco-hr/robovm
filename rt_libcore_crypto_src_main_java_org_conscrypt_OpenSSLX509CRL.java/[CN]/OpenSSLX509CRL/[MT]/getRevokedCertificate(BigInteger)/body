{
  final long revokedRef=NativeCrypto.X509_CRL_get0_by_serial(mContext,serialNumber.toByteArray());
  if (revokedRef == 0) {
    return null;
  }
  return new OpenSSLX509CRLEntry(NativeCrypto.X509_REVOKED_dup(revokedRef));
}
