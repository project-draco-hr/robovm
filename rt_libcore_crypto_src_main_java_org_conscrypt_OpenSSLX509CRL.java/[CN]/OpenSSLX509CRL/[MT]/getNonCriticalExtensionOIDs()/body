{
  String[] nonCritOids=NativeCrypto.get_X509_CRL_ext_oids(mContext,NativeCrypto.EXTENSION_TYPE_NON_CRITICAL);
  if ((nonCritOids.length == 0) && (NativeCrypto.get_X509_CRL_ext_oids(mContext,NativeCrypto.EXTENSION_TYPE_CRITICAL).length == 0)) {
    return null;
  }
  return new HashSet<String>(Arrays.asList(nonCritOids));
}
