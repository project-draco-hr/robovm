{
  byte[] d=digest.digest();
  if (!MessageDigest.isEqual(d,Base64.decode(hash))) {
    throw new SecurityException(Messages.getString("archive.32",new Object[]{JarFile.MANIFEST_NAME,name,jarName}));
  }
  verifiedEntries.put(name,certificates);
}
