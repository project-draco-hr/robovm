{
  ZipEntry ent=zip.getEntry(f);
  try {
    if (ent != null) {
      return readbytes(zip.getInputStream(ent),(int)ent.getSize());
    }
 else {
      return null;
    }
  }
 catch (  IOException x) {
    x.printStackTrace();
    return null;
  }
}
