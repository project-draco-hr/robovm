{
  checkLevel(SIGNATURES);
  SootMethod ret=(SootMethod)(subSigToMethods == null ? null : subSigToMethods.get(subsignature));
  if (ret == null)   throw new RuntimeException("No method " + subsignature + " in class "+ getName());
 else   return ret;
}
