{
  if (size == 0) {
    return -1;
  }
  if (o == null) {
    for (int i=start; i < start + size; i++) {
      if (data[i] == null) {
        return i;
      }
    }
  }
 else {
    for (int i=start; i < start + size; i++) {
      if (o.equals(data[i])) {
        return i;
      }
    }
  }
  return -1;
}
