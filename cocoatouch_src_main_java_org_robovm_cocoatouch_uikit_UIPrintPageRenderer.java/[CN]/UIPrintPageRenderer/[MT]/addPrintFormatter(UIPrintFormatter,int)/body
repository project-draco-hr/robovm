{
  if (customClass) {
    objc_addPrintFormatterSuper(getSuper(),addPrintFormatter$startingAtPageAtIndex$,formatter,pageIndex);
  }
 else {
    objc_addPrintFormatter(this,addPrintFormatter$startingAtPageAtIndex$,formatter,pageIndex);
  }
}
