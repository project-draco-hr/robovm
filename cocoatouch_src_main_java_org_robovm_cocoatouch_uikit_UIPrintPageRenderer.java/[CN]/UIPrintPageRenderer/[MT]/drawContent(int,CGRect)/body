{
  if (customClass) {
    objc_drawContentSuper(getSuper(),this,drawContentForPageAtIndex$inRect$,index,contentRect);
  }
 else {
    objc_drawContent(this,drawContentForPageAtIndex$inRect$,index,contentRect);
  }
}
