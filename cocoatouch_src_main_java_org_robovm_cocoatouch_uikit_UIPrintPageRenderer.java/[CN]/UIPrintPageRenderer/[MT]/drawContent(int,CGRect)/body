{
  if (customClass) {
    objc_drawContentSuper(getSuper(),drawContentForPageAtIndex$inRect$,index,contentRect);
  }
 else {
    objc_drawContent(this,drawContentForPageAtIndex$inRect$,index,contentRect);
  }
}
