{
  if (customClass) {
    objc_drawPrintFormatterSuper(getSuper(),drawPrintFormatter$forPageAtIndex$,printFormatter,index);
  }
 else {
    objc_drawPrintFormatter(this,drawPrintFormatter$forPageAtIndex$,printFormatter,index);
  }
}
