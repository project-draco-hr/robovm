{
  if (customClass) {
    objc_drawPrintFormatterSuper(getSuper(),this,drawPrintFormatter$forPageAtIndex$,printFormatter,index);
  }
 else {
    objc_drawPrintFormatter(this,drawPrintFormatter$forPageAtIndex$,printFormatter,index);
  }
}
