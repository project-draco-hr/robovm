{
  if (customClass) {
    objc_drawFooterSuper(getSuper(),drawFooterForPageAtIndex$inRect$,index,footerRect);
  }
 else {
    objc_drawFooter(this,drawFooterForPageAtIndex$inRect$,index,footerRect);
  }
}
