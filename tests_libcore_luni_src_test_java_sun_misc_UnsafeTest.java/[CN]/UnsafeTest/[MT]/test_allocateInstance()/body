{
  AllocateInstanceTestClass i=(AllocateInstanceTestClass)getUnsafe().allocateInstance(AllocateInstanceTestClass.class);
  assertEquals(0,i.i);
  assertEquals(null,i.s);
  assertEquals(i,i.getThis());
}
