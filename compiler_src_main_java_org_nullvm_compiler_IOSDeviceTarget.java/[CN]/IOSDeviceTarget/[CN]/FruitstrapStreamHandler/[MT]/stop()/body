{
  if (errThread != null) {
    errThread.interrupt();
    try {
      errThread.join();
    }
 catch (    InterruptedException e) {
    }
  }
  if (outThread != null) {
    outThread.interrupt();
    try {
      outThread.join();
    }
 catch (    InterruptedException e) {
    }
  }
}
