{
  if (!doInput) {
    throw new ProtocolException(Messages.getString("luni.28"));
  }
  connect();
  doRequest();
  if (responseCode >= HTTP_BAD_REQUEST) {
    throw new FileNotFoundException(url.toString());
  }
  return uis;
}
