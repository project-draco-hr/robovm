{
  if (closed) {
    return;
  }
  closed=true;
  if (writeToSocket) {
    if (limit > 0 && fixedMod) {
      throw new IOException(Messages.getString("luni.25"));
    }
    sendCache(closed);
  }
  disconnect(false);
}
