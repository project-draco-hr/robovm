{
  if (bytesRemaining <= 0) {
    readChunkSize();
  }
  if (atEnd) {
    disconnect(false);
    return -1;
  }
  bytesRemaining--;
  int result=is.read();
  if (useCaches && null != cacheOut) {
    cacheOut.write(result);
  }
  return result;
}
