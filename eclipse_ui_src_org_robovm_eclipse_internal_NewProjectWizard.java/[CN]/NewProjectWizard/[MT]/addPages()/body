{
  if (page1 == null) {
    page1=createPageOne();
    page1.setTitle(page1.getTitle().replace("Java","RoboVM"));
  }
  addPage(page1);
  if (page2 == null) {
    page2=new NewJavaProjectWizardPageTwo(page1);
  }
  addPage(page2);
}
