{
  if (customClass) {
    objc_acceptInputForModeSuper(getSuper(),acceptInputForMode$beforeDate$,mode,limitDate);
  }
 else {
    objc_acceptInputForMode(this,acceptInputForMode$beforeDate$,mode,limitDate);
  }
}
