{
  if (customClass) {
    objc_performSuper(getSuper(),performSelector$target$argument$order$modes$,aSelector,target,anArgument,order,modes);
  }
 else {
    objc_perform(this,performSelector$target$argument$order$modes$,aSelector,target,anArgument,order,modes);
  }
}
