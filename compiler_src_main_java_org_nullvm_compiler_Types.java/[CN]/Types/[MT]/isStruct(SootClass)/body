{
  SootClass clazz=sc;
  while (clazz.hasSuperclass()) {
    clazz=clazz.getSuperclass();
    if ("org.nullvm.rt.bro.Struct".equals(clazz.getName())) {
      return true;
    }
  }
  return false;
}
