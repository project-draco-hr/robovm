{
  if (customClass) {
    return objc_convertRectToWindowSuper(getSuper(),convertRect$toWindow$,rect,window);
  }
 else {
    return objc_convertRectToWindow(this,convertRect$toWindow$,rect,window);
  }
}
