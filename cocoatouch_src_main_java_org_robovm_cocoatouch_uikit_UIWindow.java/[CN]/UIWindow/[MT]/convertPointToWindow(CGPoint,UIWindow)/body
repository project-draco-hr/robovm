{
  if (customClass) {
    return objc_convertPointToWindowSuper(getSuper(),this,convertPoint$toWindow$,point,window);
  }
 else {
    return objc_convertPointToWindow(this,convertPoint$toWindow$,point,window);
  }
}
