{
  StringBuilder res=new StringBuilder();
  if (routingToken != 0) {
    res.append('(');
    res.append(routingToken);
    res.append(')');
  }
  res.append(token);
  res.append(command);
  return res.toString();
}
