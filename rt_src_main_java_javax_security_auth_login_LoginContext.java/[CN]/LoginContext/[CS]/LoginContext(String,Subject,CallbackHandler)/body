{
  super();
  if (subject == null) {
    throw new LoginException(Messages.getString("auth.03"));
  }
  if (cbHandler == null) {
    throw new LoginException(Messages.getString("auth.34"));
  }
  init(name,subject,cbHandler,null);
}
