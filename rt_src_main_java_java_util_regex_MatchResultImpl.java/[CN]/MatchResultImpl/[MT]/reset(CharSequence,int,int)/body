{
  valid=false;
  mode=Matcher.MODE_MATCH;
  Arrays.fill(groupBounds,-1);
  Arrays.fill(consumers,-1);
  if (newSequence != null)   this.string=newSequence;
  if (leftBound >= 0)   this.setBounds(leftBound,rightBound);
  this.startIndex=this.leftBound;
}
