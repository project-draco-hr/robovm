{
  if (!valid) {
    throw new IllegalStateException(Messages.getString("regex.05"));
  }
  if (group < 0 || group > groupCount) {
    throw new IndexOutOfBoundsException(Messages.getString("regex.06",group));
  }
}
