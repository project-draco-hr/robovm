{
  MatchResultImpl res=new MatchResultImpl(this.string,this.leftBound,this.rightBound,this.groupCount - 1,0,0);
  res.valid=valid;
  if (valid) {
    System.arraycopy(groupBounds,0,res.groupBounds,0,this.groupBounds.length);
  }
  return res;
}
