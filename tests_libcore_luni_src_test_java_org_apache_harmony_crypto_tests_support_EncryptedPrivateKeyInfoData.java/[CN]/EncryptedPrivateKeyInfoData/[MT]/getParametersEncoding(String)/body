{
  String algNameUC=algName.toUpperCase();
  byte[] ret=validAPEncodings.get(algNameUC);
  if (ret != null) {
    return ret;
  }
  throw new NoSuchAlgorithmException("No AP encoding available for " + algName);
}
