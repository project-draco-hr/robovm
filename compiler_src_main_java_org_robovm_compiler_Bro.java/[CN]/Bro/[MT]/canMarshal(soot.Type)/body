{
  if (!needsMarshaler(t)) {
    return true;
  }
  if (t instanceof RefType) {
    if (isEnum(t)) {
      return true;
    }
    return getMarshalerAnnotation(((RefType)t).getSootClass()) != null;
  }
  return false;
}
