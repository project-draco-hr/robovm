{
synchronized (this) {
synchronized (unmodifiableKeys) {
synchronized (selectedKeys) {
        SelectionKeyImpl ski=(SelectionKeyImpl)sk;
        delKey(ski);
        addKey(ski);
      }
    }
  }
}
