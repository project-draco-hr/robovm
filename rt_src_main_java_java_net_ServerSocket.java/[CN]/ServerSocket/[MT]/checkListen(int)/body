{
  if (aPort < 0 || aPort > 65535) {
    throw new IllegalArgumentException(Messages.getString("luni.56",aPort));
  }
  SecurityManager security=System.getSecurityManager();
  if (security != null) {
    security.checkListen(aPort);
  }
}
