{
  if (customClass) {
    objc_setDirectionalLockEnabledSuper(getSuper(),this,setDirectionalLockEnabled$,directionalLockEnabled);
  }
 else {
    objc_setDirectionalLockEnabled(this,setDirectionalLockEnabled$,directionalLockEnabled);
  }
}
