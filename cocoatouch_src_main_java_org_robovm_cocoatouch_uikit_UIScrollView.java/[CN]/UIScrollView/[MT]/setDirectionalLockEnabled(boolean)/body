{
  if (customClass) {
    objc_setDirectionalLockEnabledSuper(getSuper(),setDirectionalLockEnabled$,directionalLockEnabled);
  }
 else {
    objc_setDirectionalLockEnabled(this,setDirectionalLockEnabled$,directionalLockEnabled);
  }
}
