{
  if (customClass) {
    objc_setMaximumZoomScaleSuper(getSuper(),this,setMaximumZoomScale$,maximumZoomScale);
  }
 else {
    objc_setMaximumZoomScale(this,setMaximumZoomScale$,maximumZoomScale);
  }
}
