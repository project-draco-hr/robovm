{
  if (customClass) {
    objc_setMaximumZoomScaleSuper(getSuper(),setMaximumZoomScale$,maximumZoomScale);
  }
 else {
    objc_setMaximumZoomScale(this,setMaximumZoomScale$,maximumZoomScale);
  }
}
