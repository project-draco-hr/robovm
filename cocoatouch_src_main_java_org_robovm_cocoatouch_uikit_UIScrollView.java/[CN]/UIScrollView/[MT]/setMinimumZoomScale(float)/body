{
  if (customClass) {
    objc_setMinimumZoomScaleSuper(getSuper(),this,setMinimumZoomScale$,minimumZoomScale);
  }
 else {
    objc_setMinimumZoomScale(this,setMinimumZoomScale$,minimumZoomScale);
  }
}
