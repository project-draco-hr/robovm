{
  if (customClass) {
    objc_setMinimumZoomScaleSuper(getSuper(),setMinimumZoomScale$,minimumZoomScale);
  }
 else {
    objc_setMinimumZoomScale(this,setMinimumZoomScale$,minimumZoomScale);
  }
}
