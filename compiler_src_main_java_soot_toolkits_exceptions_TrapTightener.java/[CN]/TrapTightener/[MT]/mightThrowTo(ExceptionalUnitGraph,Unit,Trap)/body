{
  Collection dests=g.getExceptionDests(u);
  for (Iterator destIt=dests.iterator(); destIt.hasNext(); ) {
    ExceptionalUnitGraph.ExceptionDest dest=(ExceptionalUnitGraph.ExceptionDest)destIt.next();
    if (dest.getTrap() == t) {
      return true;
    }
  }
  return false;
}
