{
  int end=matchResult.getEnd(groupIndex);
  matchResult.setEnd(groupIndex,stringIndex);
  int shift=next.matches(stringIndex,testString,matchResult);
  if (shift < 0)   matchResult.setEnd(groupIndex,end);
  return shift;
}
