{
  super();
  if (cert == null) {
    throw new IllegalArgumentException(Messages.getString("auth.28"));
  }
  if (key == null) {
    throw new IllegalArgumentException(Messages.getString("auth.29"));
  }
  this.cert=cert;
  this.key=key;
}
