{
  try {
    bw.write(testString);
    bw.flush();
    assertTrue("Incorrect string written",sw.toString().equals(testString));
  }
 catch (  Exception e) {
    fail("Exception during write test");
  }
}
