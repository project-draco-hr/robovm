{
  if (n == 0) {
    return ONE;
  }
  if ((n < 0) || (n > 999999999)) {
    throw new ArithmeticException(Messages.getString("math.07"));
  }
  long newScale=scale * (long)n;
  return ((isZero()) ? zeroScaledBy(newScale) : new BigDecimal(getUnscaledValue().pow(n),toIntScale(newScale)));
}
