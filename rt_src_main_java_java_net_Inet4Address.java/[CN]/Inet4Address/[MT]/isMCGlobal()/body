{
  if (!isMulticastAddress()) {
    return false;
  }
  int address=InetAddress.bytesToInt(ipaddress,0);
  if (address >>> 8 < 0xE00001) {
    return false;
  }
  if (address >>> 24 > 0xEE) {
    return false;
  }
  return true;
}
