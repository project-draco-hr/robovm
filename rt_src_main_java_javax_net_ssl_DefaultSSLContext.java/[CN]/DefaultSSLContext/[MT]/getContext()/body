{
  if (defaultSSLContext == null) {
    defaultSSLContext=AccessController.doPrivileged(new PrivilegedAction<SSLContext>(){
      public SSLContext run(){
        return findDefault();
      }
    }
);
  }
  return defaultSSLContext;
}
