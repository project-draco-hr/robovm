{
synchronized (lock) {
    if (in == null) {
      throw new IOException(Messages.getString("luni.BA"));
    }
    try {
      return bytes.hasRemaining() || in.available() > 0;
    }
 catch (    IOException e) {
      return false;
    }
  }
}
