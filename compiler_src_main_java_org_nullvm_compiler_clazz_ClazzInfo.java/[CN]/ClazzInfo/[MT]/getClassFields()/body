{
  if (classFields == null) {
    classFields=new ArrayList<FieldInfo>();
    for (    FieldInfo f : fields) {
      if (f.isStatic()) {
        classFields.add(f);
      }
    }
  }
  return classFields;
}
