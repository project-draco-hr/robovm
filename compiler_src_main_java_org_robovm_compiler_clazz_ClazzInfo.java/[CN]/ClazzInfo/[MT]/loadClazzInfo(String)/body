{
  if (n != null) {
    Clazz c=clazz.clazzes.load(n);
    if (c != null) {
      ClazzInfo ci=c.getClazzInfo();
      if (ci != null) {
        return ci;
      }
    }
  }
  return new ClazzInfo(n);
}
