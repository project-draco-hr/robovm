{
  Invokestatic invokeValues=new Invokestatic(getInternalName(sootMethod.getDeclaringClass()),enumClassName,"values",String.format("()[L%s;",enumClassName));
  trampolines.add(invokeValues);
  Value values=call(fn,invokeValues.getFunctionRef(),env);
  Invokestatic invokeToObject=new Invokestatic(getInternalName(sootMethod.getDeclaringClass()),marshalerClassName,"toObject",String.format("([Ljava/lang/Enum;%sJ)Ljava/lang/Enum;",getDescriptor(nativeValue.getType())));
  trampolines.add(invokeToObject);
  return call(fn,invokeToObject.getFunctionRef(),env,values,nativeValue,new IntegerConstant(flags));
}
