{
  Invokestatic invokestatic=new Invokestatic(getInternalName(sootMethod.getDeclaringClass()),marshalerClassName,"toNative","(Ljava/lang/Object;)" + getDescriptor(nativeType));
  trampolines.add(invokestatic);
  return call(fn,invokestatic.getFunctionRef(),env,object);
}
