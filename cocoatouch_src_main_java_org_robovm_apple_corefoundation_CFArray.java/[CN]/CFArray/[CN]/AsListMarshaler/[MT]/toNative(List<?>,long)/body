{
  if (l == null) {
    return 0L;
  }
  CFArray o=null;
  if (l instanceof CFArray) {
    o=(CFArray)l;
  }
 else {
    o=CFArray.create((List<CFType>)l);
  }
  return CFType.Marshaler.toNative(o,flags);
}
