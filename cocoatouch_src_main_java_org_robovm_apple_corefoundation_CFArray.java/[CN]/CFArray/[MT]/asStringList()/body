{
  List<String> list=new ArrayList<>();
  if (size() == 0)   return list;
  if (!(get(0,CFType.class) instanceof CFString))   throw new UnsupportedOperationException("items must be of type CFString");
  for (int i=0; i < size(); i++) {
    list.add(get(i,CFString.class).toString());
  }
  return list;
}
