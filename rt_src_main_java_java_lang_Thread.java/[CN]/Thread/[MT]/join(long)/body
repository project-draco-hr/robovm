{
  if (millis == 0) {
    while (isAlive()) {
      wait();
    }
  }
 else {
    long end=System.currentTimeMillis() + millis;
    while (isAlive()) {
      wait(millis);
      millis=end - System.currentTimeMillis();
      if (millis <= 0) {
        break;
      }
    }
  }
}
