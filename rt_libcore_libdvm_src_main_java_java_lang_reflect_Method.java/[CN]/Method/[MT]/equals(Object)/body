{
  if (this == object) {
    return true;
  }
  if (!(object instanceof Method)) {
    return false;
  }
  Method rhs=(Method)object;
  return declaringClass.equals(rhs.declaringClass) && name.equals(rhs.name) && getModifiers() == rhs.getModifiers() && returnType.equals(rhs.returnType) && Arrays.equals(parameterTypes,rhs.parameterTypes);
}
