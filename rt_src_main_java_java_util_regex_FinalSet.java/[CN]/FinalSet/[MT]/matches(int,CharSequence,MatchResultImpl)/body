{
  if (matchResult.mode() == Matcher.MODE_FIND || stringIndex == matchResult.getRightBound()) {
    matchResult.setValid();
    matchResult.setEnd(0,stringIndex);
    return stringIndex;
  }
  return -1;
}
