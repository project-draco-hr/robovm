{
  if (permission == _PRIVATE_CREDENTIALS) {
    return new SecureIterator(elements.iterator()){
      @Override public SST next(){
        SST obj=iterator.next();
        checkPermission(new PrivateCredentialPermission(obj.getClass().getName(),principals));
        return obj;
      }
    }
;
  }
  return new SecureIterator(elements.iterator());
}
