{
  if (null != this.os) {
    try {
      if (null != this.writer) {
        this.writer.flush();
      }
 else {
        this.os.flush();
      }
    }
 catch (    Exception e) {
      getErrorManager().error(Messages.getString("logging.16"),e,ErrorManager.FLUSH_FAILURE);
    }
  }
}
