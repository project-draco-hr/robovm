{
  String qualifiers=Modifier.toString(modifiers) + " " + type.toString();
  qualifiers=qualifiers.trim();
  if (qualifiers.equals(""))   return Scene.v().quotedNameOf(name);
 else   return qualifiers + " " + Scene.v().quotedNameOf(name)+ "";
}
