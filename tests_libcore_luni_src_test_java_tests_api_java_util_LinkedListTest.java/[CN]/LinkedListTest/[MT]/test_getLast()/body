{
  assertTrue("Returned incorrect first element",ll.getLast().equals(objArray[objArray.length - 1]));
  ll.clear();
  try {
    ll.getLast();
    fail("NoSuchElementException expected");
  }
 catch (  NoSuchElementException e) {
  }
}
