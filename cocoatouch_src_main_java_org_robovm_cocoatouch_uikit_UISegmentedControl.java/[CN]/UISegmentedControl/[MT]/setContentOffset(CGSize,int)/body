{
  if (customClass) {
    objc_setContentOffsetSuper(getSuper(),setContentOffset$forSegmentAtIndex$,offset,segment);
  }
 else {
    objc_setContentOffset(this,setContentOffset$forSegmentAtIndex$,offset,segment);
  }
}
