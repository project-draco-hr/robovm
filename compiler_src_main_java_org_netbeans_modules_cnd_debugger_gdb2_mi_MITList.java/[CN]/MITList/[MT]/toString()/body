{
  String s=new String();
  if (!topLevel)   s+=isList() ? "[" : "{";
  for (int vx=0; vx < list.size(); vx++) {
    MITListItem item=list.get(vx);
    s+=item.toString();
    if (vx + 1 < list.size())     s+=",";
  }
  if (!topLevel)   s+=isList() ? "]" : "}";
  return s;
}
