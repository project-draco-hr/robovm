{
  try {
    rq.wait(1000);
  }
 catch (  Exception e) {
  }
synchronized (rq) {
    integer=new Integer(667);
    SoftReference sr=new SoftReference(integer,rq);
    sr.enqueue();
    rq.notify();
  }
}
