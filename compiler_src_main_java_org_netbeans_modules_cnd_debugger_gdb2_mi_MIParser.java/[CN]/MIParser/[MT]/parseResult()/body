{
  Token tsym=getToken();
  if (tsym.type != TokenType.SYM)   error("result","variable",tsym);
  Token teq=getToken();
  if (teq.type != TokenType.EQ)   error("result","=",teq);
  MIValue value=parseValue("file".equals(tsym.value) || "fullname".equals(tsym.value));
  return new MIResult(tsym.value,value);
}
