{
  MITList list=new MITList(endToken == TokenType.RB,topLevel);
  while (true) {
    MIResult result=parseResult();
    list.add(result);
    Token t=getToken();
    if (t.type == endToken)     break;
 else     if (t.type == TokenType.COMMA)     continue;
 else     error("result list",", or ]",t);
  }
  return list;
}
