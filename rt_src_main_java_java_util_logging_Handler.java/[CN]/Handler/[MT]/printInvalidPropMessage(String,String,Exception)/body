{
  String msg=new StringBuilder().append(Messages.getString("logging.12")).append(prefix).append(":").append(key).append("/").append(value).toString();
  errorMan.error(msg,e,ErrorManager.GENERIC_FAILURE);
}
