{
  for (int i=0; i < len; ++i) {
    atBlock[atBlockPos]=in[inOff + i];
    if (++atBlockPos == BLOCK_SIZE) {
      gHASHBlock(S_at,atBlock);
      atBlockPos=0;
      atLength+=BLOCK_SIZE;
    }
  }
}
