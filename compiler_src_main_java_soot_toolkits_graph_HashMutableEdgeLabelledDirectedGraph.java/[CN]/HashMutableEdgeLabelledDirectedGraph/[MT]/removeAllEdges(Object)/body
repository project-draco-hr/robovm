{
  if (!containsAnyEdge(label))   return;
  List<DGEdge> edges=labelToEdges.get(label);
  if (edges == null)   throw new RuntimeException("label " + label + " not in graph!");
  for (  DGEdge edge : edges) {
    removeEdge(edge.from(),edge.to(),label);
  }
}
