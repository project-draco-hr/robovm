{
  if (time < 0) {
    throw new IllegalArgumentException(Messages.getString("luni.B2"));
  }
  SecurityManager security=System.getSecurityManager();
  if (security != null) {
    security.checkWrite(path);
  }
  return (setLastModifiedImpl(properPath(true),time));
}
