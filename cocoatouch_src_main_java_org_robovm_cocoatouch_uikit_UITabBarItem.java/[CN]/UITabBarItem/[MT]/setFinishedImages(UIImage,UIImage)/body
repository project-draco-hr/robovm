{
  if (customClass) {
    objc_setFinishedImagesSuper(getSuper(),this,setFinishedSelectedImage$withFinishedUnselectedImage$,selectedImage,unselectedImage);
  }
 else {
    objc_setFinishedImages(this,setFinishedSelectedImage$withFinishedUnselectedImage$,selectedImage,unselectedImage);
  }
}
