{
  this.buf=buf;
  if (!readHeader() || (ver != null && !name.equals(ver))) {
    throw new IOException(Messages.getString("archive.2D",ver));
  }
  main.put(name,value);
  while (readHeader()) {
    main.put(name,value);
  }
}
