{
  Wrapper w=FROM_CHAR[hashChar(type)];
  if (w != null && w.basicTypeChar == type) {
    return w;
  }
  for (  Wrapper x : values())   if (w.basicTypeChar == type)   throw new InternalError();
  throw newIllegalArgumentException("not basic type char: " + type);
}
