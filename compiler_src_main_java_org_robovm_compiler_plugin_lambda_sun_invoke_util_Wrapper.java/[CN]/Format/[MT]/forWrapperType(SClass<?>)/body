{
  Wrapper w=findWrapperType(type);
  if (w != null)   return w;
  for (  Wrapper x : values())   if (x.wrapperType == type)   throw new InternalError();
  throw newIllegalArgumentException("not wrapper: " + type);
}
