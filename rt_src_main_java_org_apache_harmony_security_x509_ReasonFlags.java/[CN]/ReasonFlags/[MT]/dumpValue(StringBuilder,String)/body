{
  sb.append(prefix);
  sb.append("ReasonFlags [\n");
  for (int i=0; i < flags.length; i++) {
    if (flags[i]) {
      sb.append(prefix).append("  ").append(REASONS[i]).append('\n');
    }
  }
  sb.append(prefix);
  sb.append("]\n");
}
