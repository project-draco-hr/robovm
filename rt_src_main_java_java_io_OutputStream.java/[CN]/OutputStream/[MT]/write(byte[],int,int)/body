{
  if (offset > buffer.length || offset < 0 || count < 0 || count > buffer.length - offset) {
    throw new IndexOutOfBoundsException(Messages.getString("luni.13"));
  }
  for (int i=offset; i < offset + count; i++) {
    write(buffer[i]);
  }
}
