{
  for (int i=0; i < string.length(); i++) {
    if (string.charAt(i) != testString.charAt(strIndex + i) && Pattern.getSupplement(string.charAt(i)) != testString.charAt(strIndex + i)) {
      return -1;
    }
  }
  return string.length();
}
