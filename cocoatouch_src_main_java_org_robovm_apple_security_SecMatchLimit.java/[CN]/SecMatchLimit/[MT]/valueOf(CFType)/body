{
  for (  SecMatchLimit v : values) {
    if (v.value().equals(value)) {
      return v;
    }
  }
  throw new IllegalArgumentException("No constant with value " + value + " found in "+ SecMatchLimit.class.getName());
}
