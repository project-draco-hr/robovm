{
  int[] results=new int[6];
  results[0]=(int)l;
  results[1]=(int)f;
  results[2]=(int)d;
  results[3]=(int)(1.0f / zero);
  results[4]=(int)(-1.0f / zero);
  results[5]=(int)((1.0f / zero) / (1.0f / zero));
  return results;
}
