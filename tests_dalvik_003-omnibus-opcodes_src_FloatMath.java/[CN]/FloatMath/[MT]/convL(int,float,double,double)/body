{
  long[] results=new long[6];
  results[0]=(long)i;
  results[1]=(long)f;
  results[2]=(long)d;
  results[3]=(long)(1.0 / zero);
  results[4]=(long)(-1.0 / zero);
  results[5]=(long)((1.0 / zero) / (1.0 / zero));
  return results;
}
