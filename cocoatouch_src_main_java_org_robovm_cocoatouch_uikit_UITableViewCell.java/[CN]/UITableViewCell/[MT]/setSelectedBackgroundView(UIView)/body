{
  if (customClass) {
    objc_setSelectedBackgroundViewSuper(getSuper(),this,setSelectedBackgroundView$,selectedBackgroundView);
  }
 else {
    objc_setSelectedBackgroundView(this,setSelectedBackgroundView$,selectedBackgroundView);
  }
}
