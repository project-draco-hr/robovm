{
  if (customClass) {
    objc_setSelectedBackgroundViewSuper(getSuper(),setSelectedBackgroundView$,selectedBackgroundView);
  }
 else {
    objc_setSelectedBackgroundView(this,setSelectedBackgroundView$,selectedBackgroundView);
  }
}
