{
  assertNull(new MyX509Certificate().getExtendedKeyUsage());
  List<String> l=cert.getExtendedKeyUsage();
  assertNotNull(l);
  try {
    l.clear();
  }
 catch (  Exception e) {
  }
  try {
    l.add("Test");
  }
 catch (  Exception e) {
  }
  try {
    if (l.size() > 0) {
      l.remove(0);
    }
  }
 catch (  Exception e) {
  }
}
