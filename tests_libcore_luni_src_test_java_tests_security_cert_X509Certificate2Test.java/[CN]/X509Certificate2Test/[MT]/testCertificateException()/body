{
  try {
    generateCert(CERT_TAMPERED);
    fail();
  }
 catch (  CertificateException expected) {
  }
  try {
    generateCert(CERT);
    fail();
  }
 catch (  CertificateException expected) {
  }
}
