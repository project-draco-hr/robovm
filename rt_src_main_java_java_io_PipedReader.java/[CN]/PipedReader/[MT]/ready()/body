{
synchronized (lock) {
    if (!isConnected) {
      throw new IOException(Messages.getString("luni.D1"));
    }
    if (data == null) {
      throw new IOException(Messages.getString("luni.CF"));
    }
    return in != -1;
  }
}
