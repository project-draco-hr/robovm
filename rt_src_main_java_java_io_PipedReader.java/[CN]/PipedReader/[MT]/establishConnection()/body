{
synchronized (lock) {
    if (data == null) {
      throw new IOException(Messages.getString("luni.CF"));
    }
    if (isConnected) {
      throw new IOException(Messages.getString("luni.D0"));
    }
    isConnected=true;
  }
}
