{
  if (customClass) {
    objc_beginGeneratingDeviceOrientationNotificationsSuper(getSuper(),this,beginGeneratingDeviceOrientationNotifications);
  }
 else {
    objc_beginGeneratingDeviceOrientationNotifications(this,beginGeneratingDeviceOrientationNotifications);
  }
}
