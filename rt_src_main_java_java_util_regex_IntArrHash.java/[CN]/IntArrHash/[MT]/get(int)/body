{
  int hashCode=key & mask;
  int i=0;
  int storedKey;
  for (; ; ) {
    storedKey=table[hashCode];
    if (storedKey == 0) {
      return null;
    }
    if (storedKey == key) {
      return (int[])values[hashCode];
    }
    i++;
    i&=mask;
    hashCode+=i;
    hashCode&=mask;
  }
}
