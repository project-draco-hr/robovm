{
  int i=0;
  int hashCode=key & mask;
  for (; ; ) {
    if (table[hashCode] == 0 || table[hashCode] == key) {
      table[hashCode]=key;
      values[hashCode]=value;
      return;
    }
    i++;
    i&=mask;
    hashCode+=i;
    hashCode&=mask;
  }
}
