{
  try {
    projectProperties.storePreferences(project);
  }
 catch (  BackingStoreException e) {
    MessageDialog.openError(RoboVMPlugin.getShell(),"Failed to store properties",e.getMessage());
    return false;
  }
  return true;
}
