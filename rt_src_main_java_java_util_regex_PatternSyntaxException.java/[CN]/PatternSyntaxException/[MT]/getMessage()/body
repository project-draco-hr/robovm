{
  String filler="";
  if (index >= 1) {
    char[] temp=new char[index];
    Arrays.fill(temp,' ');
    filler=new String(temp);
  }
  return desc + ((pattern != null && pattern.length() != 0) ? Messages.getString("regex.07",new Object[]{index,pattern,filler}) : "");
}
