{
  if (value == null)   throw new NullPointerException("value");
  for (  CFHTTPVersion v : values) {
    if (v.value().equals(value)) {
      return v;
    }
  }
  throw new IllegalArgumentException("No constant with value " + value + " found in "+ CFHTTPVersion.class.getName());
}
