{
  if (wrapCount < 1) {
    throw new IllegalArgumentException("wrapCount < 1");
  }
  if (n < 1) {
    throw new IllegalArgumentException("n < 1");
  }
  return toPtr(cls,VM.allocateMemory(Ptr.sizeOf() * n),wrapCount,lazy());
}
