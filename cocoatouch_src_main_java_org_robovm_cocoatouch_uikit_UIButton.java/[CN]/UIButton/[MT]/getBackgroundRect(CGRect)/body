{
  if (customClass) {
    return objc_getBackgroundRectSuper(getSuper(),this,backgroundRectForBounds$,bounds);
  }
 else {
    return objc_getBackgroundRect(this,backgroundRectForBounds$,bounds);
  }
}
