{
  KeyStoreBuilder ksb;
  try {
    ksb=new KeyStoreBuilder();
    assertNotNull(ksb);
    ksb.getKeyStore();
    ksb.getProtectionParameter("test");
  }
 catch (  Exception e) {
    fail("Unexpected exception " + e.getMessage());
  }
}
