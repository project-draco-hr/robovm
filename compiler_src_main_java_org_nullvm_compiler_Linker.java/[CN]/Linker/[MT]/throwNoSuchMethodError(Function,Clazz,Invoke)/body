{
  call(f,NVM_BC_THROW_NO_SUCH_METHOD_ERROR,f.getParameterRef(0),getString(String.format("%s.%s%s",target.getClassName(),invoke.getMethodName(),invoke.getMethodDesc())));
  f.add(new Unreachable());
}
