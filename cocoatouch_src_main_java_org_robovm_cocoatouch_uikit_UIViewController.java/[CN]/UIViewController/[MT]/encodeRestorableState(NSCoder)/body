{
  if (customClass) {
    objc_encodeRestorableStateSuper(getSuper(),encodeRestorableStateWithCoder$,coder);
  }
 else {
    objc_encodeRestorableState(this,encodeRestorableStateWithCoder$,coder);
  }
}
