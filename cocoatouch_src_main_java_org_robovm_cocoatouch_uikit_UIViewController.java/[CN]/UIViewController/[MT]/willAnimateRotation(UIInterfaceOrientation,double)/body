{
  if (customClass) {
    objc_willAnimateRotationSuper(getSuper(),this,willAnimateRotationToInterfaceOrientation$duration$,interfaceOrientation,duration);
  }
 else {
    objc_willAnimateRotation(this,willAnimateRotationToInterfaceOrientation$duration$,interfaceOrientation,duration);
  }
}
