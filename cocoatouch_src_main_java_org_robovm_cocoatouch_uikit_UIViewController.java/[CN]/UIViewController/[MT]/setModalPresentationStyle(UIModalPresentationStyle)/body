{
  if (customClass) {
    objc_setModalPresentationStyleSuper(getSuper(),this,setModalPresentationStyle$,modalPresentationStyle);
  }
 else {
    objc_setModalPresentationStyle(this,setModalPresentationStyle$,modalPresentationStyle);
  }
}
