{
  if (customClass) {
    objc_dismissViewControllerSuper(getSuper(),this,dismissViewControllerAnimated$completion$,flag,completion);
  }
 else {
    objc_dismissViewController(this,dismissViewControllerAnimated$completion$,flag,completion);
  }
}
