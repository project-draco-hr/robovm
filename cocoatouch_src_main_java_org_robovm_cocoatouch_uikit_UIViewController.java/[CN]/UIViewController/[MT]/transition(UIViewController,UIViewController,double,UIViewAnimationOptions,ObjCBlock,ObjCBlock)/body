{
  if (customClass) {
    objc_transitionSuper(getSuper(),transitionFromViewController$toViewController$duration$options$animations$completion$,fromViewController,toViewController,duration,options,animations,completion);
  }
 else {
    objc_transition(this,transitionFromViewController$toViewController$duration$options$animations$completion$,fromViewController,toViewController,duration,options,animations,completion);
  }
}
