{
  if (customClass) {
    objc_willRotateSuper(getSuper(),willRotateToInterfaceOrientation$duration$,toInterfaceOrientation,duration);
  }
 else {
    objc_willRotate(this,willRotateToInterfaceOrientation$duration$,toInterfaceOrientation,duration);
  }
}
