{
  if (customClass) {
    objc_performSegueSuper(getSuper(),this,performSegueWithIdentifier$sender$,identifier,sender);
  }
 else {
    objc_performSegue(this,performSegueWithIdentifier$sender$,identifier,sender);
  }
}
