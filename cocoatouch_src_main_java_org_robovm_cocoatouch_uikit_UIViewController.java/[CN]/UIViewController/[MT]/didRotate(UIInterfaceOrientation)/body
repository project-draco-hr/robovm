{
  if (customClass) {
    objc_didRotateSuper(getSuper(),didRotateFromInterfaceOrientation$,fromInterfaceOrientation);
  }
 else {
    objc_didRotate(this,didRotateFromInterfaceOrientation$,fromInterfaceOrientation);
  }
}
