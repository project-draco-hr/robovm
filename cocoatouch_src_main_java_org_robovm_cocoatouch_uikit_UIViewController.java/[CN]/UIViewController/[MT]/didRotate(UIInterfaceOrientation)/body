{
  if (customClass) {
    objc_didRotateSuper(getSuper(),this,didRotateFromInterfaceOrientation$,fromInterfaceOrientation);
  }
 else {
    objc_didRotate(this,didRotateFromInterfaceOrientation$,fromInterfaceOrientation);
  }
}
