{
  IEclipsePreferences node=new ProjectScope(project).getNode(NullVMPlugin.PLUGIN_ID);
  setArch(node.get(NullVMPlugin.PREFERENCE_INCREMENTAL_BUILD_ARCH,null));
  setOs(node.get(NullVMPlugin.PREFERENCE_INCREMENTAL_BUILD_OS,null));
}
