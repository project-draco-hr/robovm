{
  if (item == null)   throw new RuntimeException("Bad idea! You tried to remove " + " a null object from a Chain!");
  stateCount++;
  if (map.get(item) != null) {
    Link link=map.get(item);
    link.unlinkSelf();
    map.remove(item);
    return true;
  }
  return false;
}
