{
  List<TypeVariable> parents=var.parents();
  for (  TypeVariable parent : parents) {
    if (!black.contains(parent)) {
      black.add(parent);
      dfsg_visit(parent);
    }
  }
  finished.add(0,var);
}
