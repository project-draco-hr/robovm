{
  String s=new String();
  if (token != 0)   s+="" + token;
  s+=type;
  if (isStream) {
    s+="\"" + stream + "\"";
  }
 else {
    if (results.isEmpty())     s+=cls;
 else     s+=cls + "," + results.toString();
  }
  if (isError)   s+=" ERROR: " + error;
  return s;
}
