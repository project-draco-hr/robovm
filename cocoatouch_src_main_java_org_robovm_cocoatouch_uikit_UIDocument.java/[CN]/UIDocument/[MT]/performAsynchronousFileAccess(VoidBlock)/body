{
  if (customClass) {
    objc_performAsynchronousFileAccessSuper(getSuper(),this,performAsynchronousFileAccessUsingBlock$,block);
  }
 else {
    objc_performAsynchronousFileAccess(this,performAsynchronousFileAccessUsingBlock$,block);
  }
}
