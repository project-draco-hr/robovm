{
  if (customClass) {
    objc_userInteractionNoLongerPermittedForErrorSuper(getSuper(),userInteractionNoLongerPermittedForError$,error);
  }
 else {
    objc_userInteractionNoLongerPermittedForError(this,userInteractionNoLongerPermittedForError$,error);
  }
}
