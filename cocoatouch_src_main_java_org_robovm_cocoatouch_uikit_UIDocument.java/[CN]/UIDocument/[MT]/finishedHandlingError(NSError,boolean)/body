{
  if (customClass) {
    objc_finishedHandlingErrorSuper(getSuper(),this,finishedHandlingError$recovered$,error,recovered);
  }
 else {
    objc_finishedHandlingError(this,finishedHandlingError$recovered$,error,recovered);
  }
}
