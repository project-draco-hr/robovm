{
  if (customClass) {
    objc_performBatchUpdatesSuper(getSuper(),this,performBatchUpdates$completion$,updates,completion);
  }
 else {
    objc_performBatchUpdates(this,performBatchUpdates$completion$,updates,completion);
  }
}
