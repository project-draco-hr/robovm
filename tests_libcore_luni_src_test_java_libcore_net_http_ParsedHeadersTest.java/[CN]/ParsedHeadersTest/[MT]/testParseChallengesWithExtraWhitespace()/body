{
  RawHeaders headers=new RawHeaders();
  headers.add("WWW-Authenticate","  s1  realm=\"a\"  ,  s2  realm=\"b\",  ");
  assertEquals(Arrays.asList(new Challenge("s1","a"),new Challenge("s2","b")),HeaderParser.parseChallenges(headers,"WWW-Authenticate"));
}
