{
  RawHeaders headers=new RawHeaders();
  headers.add("WWW-Authenticate","s1 realm=\"ab,cd\", s2 realm=\"ef,gh\"");
  assertEquals(Arrays.asList(new Challenge("s1","ab,cd"),new Challenge("s2","ef,gh")),HeaderParser.parseChallenges(headers,"WWW-Authenticate"));
}
