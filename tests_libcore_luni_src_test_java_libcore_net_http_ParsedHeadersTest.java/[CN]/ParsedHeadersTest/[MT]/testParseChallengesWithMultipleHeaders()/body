{
  RawHeaders headers=new RawHeaders();
  headers.add("WWW-Authenticate","s1 realm=\"abc\"");
  headers.add("WWW-Authenticate","s2 realm=\"def\"");
  assertEquals(Arrays.asList(new Challenge("s1","abc"),new Challenge("s2","def")),HeaderParser.parseChallenges(headers,"WWW-Authenticate"));
}
