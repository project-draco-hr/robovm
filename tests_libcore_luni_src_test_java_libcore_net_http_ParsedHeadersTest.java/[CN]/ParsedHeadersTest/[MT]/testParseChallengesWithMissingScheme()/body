{
  RawHeaders headers=new RawHeaders();
  headers.add("WWW-Authenticate","realm=\"a\"");
  assertEquals(Collections.<Challenge>emptyList(),HeaderParser.parseChallenges(headers,"WWW-Authenticate"));
}
