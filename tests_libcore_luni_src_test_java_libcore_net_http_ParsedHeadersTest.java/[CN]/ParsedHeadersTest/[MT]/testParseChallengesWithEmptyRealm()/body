{
  RawHeaders headers=new RawHeaders();
  headers.add("WWW-Authenticate","Basic realm=\"\"");
  assertEquals(Arrays.asList(new Challenge("Basic","")),HeaderParser.parseChallenges(headers,"WWW-Authenticate"));
}
