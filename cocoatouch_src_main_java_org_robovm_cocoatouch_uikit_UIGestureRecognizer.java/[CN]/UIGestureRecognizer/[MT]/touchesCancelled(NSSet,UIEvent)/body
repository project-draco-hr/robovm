{
  if (customClass) {
    objc_touchesCancelledSuper(getSuper(),touchesCancelled$withEvent$,touches,event);
  }
 else {
    objc_touchesCancelled(this,touchesCancelled$withEvent$,touches,event);
  }
}
