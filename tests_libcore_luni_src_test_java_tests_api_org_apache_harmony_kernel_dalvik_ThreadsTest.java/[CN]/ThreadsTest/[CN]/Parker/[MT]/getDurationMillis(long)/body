{
synchronized (this) {
    if (!completed) {
      try {
        wait(maxWaitMillis);
      }
 catch (      InterruptedException ex) {
      }
      if (!completed) {
        Assert.fail("parker hanging");
      }
    }
    return endMillis - startMillis;
  }
}
