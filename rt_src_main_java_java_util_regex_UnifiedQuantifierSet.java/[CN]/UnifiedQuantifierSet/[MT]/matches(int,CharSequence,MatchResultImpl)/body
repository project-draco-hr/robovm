{
  while (stringIndex + leaf.charCount() <= matchResult.getRightBound() && leaf.accepts(stringIndex,testString) > 0)   stringIndex+=leaf.charCount();
  return next.matches(stringIndex,testString,matchResult);
}
