{
  if (customClass) {
    objc_loadDataSuper(getSuper(),this,loadData$MIMEType$textEncodingName$baseURL$,data,MIMEType,encodingName,baseURL);
  }
 else {
    objc_loadData(this,loadData$MIMEType$textEncodingName$baseURL$,data,MIMEType,encodingName,baseURL);
  }
}
