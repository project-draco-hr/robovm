{
  CFError.CFErrorPtr ptr=new CFError.CFErrorPtr();
  CFURL result=createFileReferenceURL(allocator,url,ptr);
  if (ptr.get() != null) {
    throw new CFErrorException(ptr.get());
  }
  return result;
}
