{
  assertEquals("Expected a " + expectedType.getName() + " but was "+ Arrays.toString(array),1,array.length);
  assertTrue("Expected a " + expectedType.getName() + " but was "+ array[0],expectedType.isInstance(array[0]));
  return expectedType.cast(array[0]);
}
