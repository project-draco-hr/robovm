{
  File f=new File(dir,file);
  try {
    if (!f.getCanonicalPath().startsWith(base_canon))     throw new IllegalArgumentException(file);
    if (in_cache(f))     return readbytes(new FileInputStream(f),(int)f.length());
 else     return null;
  }
 catch (  IOException x) {
    return null;
  }
}
