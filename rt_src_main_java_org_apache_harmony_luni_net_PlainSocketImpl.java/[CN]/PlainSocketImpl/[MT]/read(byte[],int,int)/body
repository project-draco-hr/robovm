{
  if (shutdownInput) {
    return -1;
  }
  int read=netImpl.read(fd,buffer,offset,count,receiveTimeout);
  if (read == 0) {
    throw new SocketTimeoutException();
  }
  if (read == -1) {
    shutdownInput=true;
  }
  return read;
}
