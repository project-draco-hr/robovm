{
  AlphabeticIndex.ImmutableIndex de=createIndex(Locale.GERMAN);
  assertHasLabel(de,"??ind","S");
  assertHasLabel(de,"Sacher","S");
  assertHasLabel(de,"Schiller","Sch");
  assertHasLabel(de,"Steiff","St");
}
