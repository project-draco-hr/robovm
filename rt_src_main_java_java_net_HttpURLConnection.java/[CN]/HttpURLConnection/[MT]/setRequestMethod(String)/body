{
  if (connected) {
    throw new ProtocolException(Messages.getString("luni.5E"));
  }
  for (int i=0; i < methodTokens.length; i++) {
    if (methodTokens[i].equals(method)) {
      this.method=methodTokens[i];
      return;
    }
  }
  throw new ProtocolException();
}
