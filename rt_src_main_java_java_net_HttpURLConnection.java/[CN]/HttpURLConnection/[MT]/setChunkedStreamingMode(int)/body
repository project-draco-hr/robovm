{
  if (super.connected) {
    throw new IllegalStateException(Messages.getString("luni.5F"));
  }
  if (0 <= fixedContentLength) {
    throw new IllegalStateException(Messages.getString("luni.60"));
  }
  if (0 >= chunklen) {
    chunkLength=DEFAULT_CHUNK_LENGTH;
  }
 else {
    chunkLength=chunklen;
  }
}
