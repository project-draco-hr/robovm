{
  System.out.println("Array.checkNegAlloc");
  String[] strings;
  int[] ints;
  try {
    ints=new int[count];
    assert(false);
  }
 catch (  NegativeArraySizeException nase) {
  }
  try {
    strings=new String[count];
    assert(false);
  }
 catch (  NegativeArraySizeException nase) {
  }
}
